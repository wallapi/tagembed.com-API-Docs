# Social Widget - nodejs starter, every file in one text file.
# Each file below starts with its own FILE line (five = signs, FILE:, its path). Generated by tools/build-previews.py.

===== FILE: .env.example =====
# Copy this file to .env and fill in ACCESS_TOKEN.
# Tagembed dashboard -> your gallery's card -> three-dot menu -> "Access Token".
ACCESS_TOKEN=
# Always this value - nothing to copy from the dashboard.
API_BASE_URL=https://api.tagembed.com/api
# Theme file name from the themes/ folder, e.g. modern-card, vivid, review-list.
WIDGET_THEME=modern-card
PORT=3000

===== FILE: README.md =====
# Social Widget - Node.js

Shows the live posts from your Tagembed gallery, in the theme you picked. Until you add your token it shows sample posts, so you can run it right away.

## Files

| File | What it does |
|---|---|
| `server.js` | The whole widget: settings, Tagembed call, cache, HTML. |
| `package.json` | Two packages: `express` and `dotenv`. |
| `themes/` | All 19 themes (`.css` is the look, `.json` is how a card is laid out). |
| `samples/` | The sample posts shown while `ACCESS_TOKEN` is empty. |
| `.env.example` | The settings - copy it to `.env`. |

Keep `preview.html` (from the chat) next to it as the design reference - it opens by double-click and needs nothing.

## Run it

1. Install Node.js 18 or newer once from https://nodejs.org (the LTS button).
2. Open a terminal in this folder (macOS: right-click the folder, **New Terminal at Folder**; Windows: open the folder, type `cmd` in the address bar, press Enter).
3. Type `npm install` and press Enter (first time only).
4. Type `npm start` and press Enter.
5. Open http://localhost:3000 in your browser. Stop it with Ctrl+C.

**On a host** (Render, Railway, a VPS, Docker...): run `npm install` then `npm start`, and set `ACCESS_TOKEN`, `API_BASE_URL` and `WIDGET_THEME` in the host's environment settings instead of `.env`. Serverless hosts (Vercel, Netlify functions) do not keep files, so the cache resets between runs - it still works, it just calls Tagembed more often.

## Settings

Copy `.env.example` to a new file named `.env` in the same folder and fill it in:

| Setting | What to put |
|---|---|
| `ACCESS_TOKEN` | Your gallery's token: Tagembed dashboard, the gallery's card, three-dot menu, **Access Token**. Leave it empty to see the sample posts. |
| `API_BASE_URL` | Always `https://api.tagembed.com/api` - nothing to copy. |
| `WIDGET_THEME` | The theme, by file name in `themes/`: `classic-card`, `social-card`, `modern-card`, `classic-photo`, `square-photo`, `collage`, `vivid`, `horizontal-slider`, `horizontal-columns`, `slider`, `reels`, `story-theme`, `single-post`, `widget-theme`, `review-box`, `review-carousel`, `review-list`, `rating-badge`, `badge`. |
| `PORT` | The port to listen on (default `3000`). |

The token stays on the server. It never appears in the page, in View Source or in the browser.

## Changing the look

- Another theme: change `WIDGET_THEME` in `.env`, then restart the server: Ctrl+C, then `npm start`.
- Your own colours, font, columns: create `custom.css` next to `server.js` with your changes. It is added after the theme, so it wins. For example:

  ```css
  :root { --tbd-bg: #fff8f0; --tbd-cols: 4; --tbd-radius: 16px; }
  ```

  The variables are listed at the top of your theme's file in `themes/`.

## How the cache works

Posts are saved in `cache/posts.json` for 5 minutes (`CACHE_TTL_SECONDS` in `server.js`), so Tagembed is called about 288 times a day however many people visit. When the saved copy runs out, one request refreshes it and everyone else keeps getting the saved copy. If Tagembed cannot be reached, the last good copy keeps showing - a stale widget beats an empty one. If the `cache` folder cannot be written, the widget still works; it just calls Tagembed on every visit (the log says so).

## If it goes wrong

- **The "Sample posts" note stays on the page** - `ACCESS_TOKEN` is empty, or the file is not named exactly `.env`, or it is not in this folder. After editing it: restart the server: Ctrl+C, then `npm start`.
- **"No posts to show yet"** - the log says why: `Tagembed API 401: ...` means a wrong token or the daily limit; no error means the gallery has no approved posts yet.
- **Wrong look** - check the spelling of `WIDGET_THEME`. An unknown name falls back to `modern-card` and says so in the log.

===== FILE: package.json =====
{
  "name": "social-widget",
  "version": "1.0.0",
  "private": true,
  "description": "Social Widget - shows the live posts from a Tagembed gallery",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}

===== FILE: server.js =====
'use strict';
// Social Widget - Node.js starter (Express).
// Renders the Tagembed gallery's posts as one finished HTML page. The token stays
// on this server; the browser only ever receives HTML.
//
// Settings (.env): ACCESS_TOKEN, API_BASE_URL, WIDGET_THEME, PORT.
// Look: themes/<WIDGET_THEME>.css, plus custom.css (optional) for your own changes.

require('dotenv').config();
const express = require('express');
const fs = require('fs');
const path = require('path');

const PORT = Number(process.env.PORT) || 3000;
const TOKEN = (process.env.ACCESS_TOKEN || '').trim();
const API_BASE_URL = (process.env.API_BASE_URL || 'https://api.tagembed.com/api').trim().replace(/\/+$/, '');
const CACHE_TTL_SECONDS = 300; // 5 minutes: about 288 API calls a day, whatever the traffic
const CACHE_DIR = path.join(__dirname, 'cache');
const CACHE_FILE = path.join(CACHE_DIR, 'posts.json');

// ---------- theme ----------

function loadTheme(wanted) {
  let slug = String(wanted || '').trim().toLowerCase();
  if (!/^[a-z-]+$/.test(slug) || !fs.existsSync(path.join(__dirname, 'themes', slug + '.json'))) {
    if (slug) console.warn(`[social-widget] Unknown WIDGET_THEME "${slug}" - using modern-card`);
    slug = 'modern-card';
  }
  const meta = JSON.parse(fs.readFileSync(path.join(__dirname, 'themes', slug + '.json'), 'utf8'));
  let css = fs.readFileSync(path.join(__dirname, 'themes', slug + '.css'), 'utf8');
  const custom = path.join(__dirname, 'custom.css');
  if (fs.existsSync(custom)) css += '\n  /* custom.css */\n' + fs.readFileSync(custom, 'utf8');
  return { meta, css };
}

const theme = loadTheme(process.env.WIDGET_THEME);

function samplePosts() {
  const file = theme.meta.type === 'review' ? 'reviews.json' : 'social.json';
  return JSON.parse(fs.readFileSync(path.join(__dirname, 'samples', file), 'utf8'));
}

// ---------- cache ----------

function readCache() {
  try {
    return JSON.parse(fs.readFileSync(CACHE_FILE, 'utf8'));
  } catch {
    return null;
  }
}

function writeCache(data) {
  try {
    fs.mkdirSync(CACHE_DIR, { recursive: true });
    // Write a temp file and rename it in, so a reader never sees half a file.
    const tmp = `${CACHE_FILE}.${process.pid}.tmp`;
    fs.writeFileSync(tmp, JSON.stringify({ savedAt: Date.now(), data }));
    fs.renameSync(tmp, CACHE_FILE);
  } catch (err) {
    // Unwritable folder: keep serving live results instead of failing.
    console.warn('[social-widget] Cache folder not writable, serving live:', err.message);
  }
}

// ---------- API ----------

async function fetchPosts() {
  const res = await fetch(`${API_BASE_URL}/v3/posts?limit=24`, {
    headers: { Authorization: `Bearer ${TOKEN}`, Accept: 'application/json' },
    signal: AbortSignal.timeout(10000),
  });
  let json = null;
  try {
    json = await res.json();
  } catch {
    // not JSON - handled below
  }
  // Check the HTTP status AND the envelope flag: status can be false on a 200.
  if (!res.ok || !json || json.status === false) {
    const detail = res.status === 422 ? JSON.stringify(json && json.body && json.body.fields)
      : (json && json.message) || res.statusText;
    throw new Error(`Tagembed API ${res.status}: ${detail}`);
  }
  const body = json.body || {};
  return { posts: Array.isArray(body.posts) ? body.posts : [], paging: body.paging || null };
}

let refreshing = null; // one refresh at a time; other requests get the old copy

async function getPosts() {
  // No token yet: show the sample posts, touch neither the API nor the cache.
  if (!TOKEN) return { posts: samplePosts(), paging: null, sample: true };

  const cached = readCache();
  if (cached && Date.now() - cached.savedAt < CACHE_TTL_SECONDS * 1000) return cached.data;

  if (!refreshing) {
    refreshing = fetchPosts()
      .then((data) => { writeCache(data); return data; })
      .catch((err) => {
        console.error('[social-widget]', err.message);
        // A failed refresh keeps serving the old copy; empty only if nothing ever loaded.
        return cached ? cached.data : { posts: [], paging: null };
      })
      .finally(() => { refreshing = null; });
  }
  return cached ? cached.data : refreshing;
}

// ---------- HTML (same markup as preview.html) ----------

const NET_MARK = {
  instagram: 'IG', facebook: 'f', twitter: 'X', x: 'X', tiktok: '♪', youtube: '▶', linkedin: 'in',
  pinterest: 'P', google: 'G', yelp: 'y', tripadvisor: 'T', trustpilot: '★',
};

function esc(value) {
  return String(value == null ? '' : value)
    .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;').replace(/'/g, '&#x27;');
}

// Only http(s) links and images - never javascript: or data: from the API.
function safeUrl(url) {
  return typeof url === 'string' && /^https?:\/\//i.test(url) ? url : '';
}

function shortDate(iso) {
  const d = new Date(iso);
  if (isNaN(d)) return '';
  return d.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric', timeZone: 'UTC' });
}

// Photo-only themes skip posts without an image - they would be empty cards.
function visiblePosts(posts, parts) {
  if (parts.join() !== 'media') return posts;
  return posts.filter((p) => Array.isArray(p.media) && p.media.some((m) => m && m.type === 'image' && safeUrl(m.cdn_url)));
}

function renderCard(post, parts) {
  const author = post.author || {};
  const network = post.network || {};
  const name = author.name || author.handle || '';
  const netName = network.name || '';
  const media = Array.isArray(post.media) ? post.media : [];
  // The first media entry of type "image" - never media[0], which can be a video.
  const image = parts.includes('media') ? media.find((m) => m && m.type === 'image' && safeUrl(m.cdn_url)) : null;
  const blocks = {};

  if (image) {
    let style = image.width && image.height ? `--tbd-ar:${Number(image.width)} / ${Number(image.height)};` : '';
    if (typeof image.preview_data_uri === 'string' && image.preview_data_uri.startsWith('data:image/')) {
      style += `--tbd-ph:url(${image.preview_data_uri})`;
    }
    const play = media.some((m) => m && m.type === 'video') ? '<span class="tbd-play"></span>' : '';
    blocks.media = `<div class="tbd-media" data-network="${esc(netName)}"${style ? ` style="${esc(style)}"` : ''}>`
      + `<img src="${esc(image.cdn_url)}" alt="" loading="lazy">${play}</div>`;
  }

  const initial = esc(name.slice(0, 1).toUpperCase());
  const avatar = safeUrl(author.avatar_url)
    ? `<img class="tbd-avatar" src="${esc(author.avatar_url)}" alt="" loading="lazy" data-initial="${initial}">`
    : `<span class="tbd-avatar">${initial}</span>`;
  const slug = String(network.slug || '').toLowerCase();
  blocks.head = `<div class="tbd-head">${avatar}<div class="tbd-who">`
    + (name ? `<span class="tbd-author">${esc(name)}</span>` : '')
    + `<time class="tbd-date" datetime="${esc(post.created_at)}">${esc(shortDate(post.created_at))}</time></div>`
    + `<span class="tbd-net" data-net="${esc(slug)}" data-mark="${esc(NET_MARK[slug] || netName.slice(0, 1))}" title="${esc(netName)}"></span></div>`;

  const rating = Number(post.rating);
  if (rating >= 1 && rating <= 5) {
    const r = Math.round(rating);
    blocks.stars = `<div class="tbd-stars" aria-label="${r} out of 5">${'★'.repeat(r)}${'☆'.repeat(5 - r)}</div>`;
  }
  const text = post.content && post.content.text;
  if (text) blocks.text = `<p class="tbd-text">${esc(text)}</p>`;

  const inner = parts.filter((p) => blocks[p]).map((p) => blocks[p]).join('\n      ');
  const cls = image || !parts.includes('media') ? 'tbd-card' : 'tbd-card tbd-card--text';
  const href = safeUrl(post.source && post.source.permalink) || '#';
  return `    <a class="${cls}" href="${esc(href)}" target="_blank" rel="noopener noreferrer">\n      ${inner}\n    </a>`;
}

// Rating Badge / Badge: the average of every rated post, its stars and the count.
function renderBadge(posts) {
  const rated = posts.filter((p) => typeof p.rating === 'number' && p.rating >= 1 && p.rating <= 5);
  const nets = [];
  rated.forEach((p) => {
    const n = p.network || {};
    if (n.slug && !nets.some((x) => x.slug === n.slug)) nets.push({ slug: n.slug, name: n.name || '' });
  });
  const avg = rated.length ? rated.reduce((sum, p) => sum + p.rating, 0) / rated.length : 0;
  const title = nets.length === 1 ? `${nets[0].name} Reviews` : 'Customer Reviews';
  const full = Math.floor(avg + 0.5);
  const marks = nets.map((n) => `<span class="tbd-net" data-net="${esc(n.slug)}" data-mark="${esc(NET_MARK[n.slug] || n.name.slice(0, 1))}" `
    + `title="${esc(n.name)}"></span>`).join('');
  return '  <div class="tbd-badge">\n'
    + `    <div class="tbd-badge-nets">${marks}</div>\n`
    + `    <div class="tbd-badge-title">${esc(title)}</div>\n`
    + `    <div class="tbd-badge-score"><span class="tbd-badge-avg">${avg.toFixed(1)}</span>`
    + `<span class="tbd-stars" aria-label="${avg.toFixed(1)} out of 5">${'★'.repeat(full)}${'☆'.repeat(5 - full)}</span></div>\n`
    + `    <div class="tbd-badge-count">Based on ${rated.length} review${rated.length === 1 ? '' : 's'}</div>\n`
    + '  </div>';
}

function renderPage(data) {
  const { meta, css } = theme;
  const posts = visiblePosts(data.posts || [], meta.parts);
  const cards = posts.map((p) => renderCard(p, meta.parts)).join('\n');
  let track = posts.length ? `  <div class="tbd-track">\n${cards}\n  </div>` : '  <p class="tbd-empty">No posts to show yet.</p>';
  if (meta.layout === 'badge' && posts.length) track = renderBadge(posts);
  const slider = meta.layout === 'slider' && posts.length;
  const body = slider
    ? `  <div class="tbd-slider">\n  <button class="tbd-arrow tbd-arrow--prev" type="button" data-dir="-1" aria-label="Previous">‹</button>\n${track}\n`
      + '  <button class="tbd-arrow tbd-arrow--next" type="button" data-dir="1" aria-label="Next">›</button>\n  </div>'
    : track;
  const script = slider
    ? `\n<script>\n  // Arrows scroll the row by one view.\n  document.querySelectorAll('.tbd-arrow').forEach(function (b) {
    b.addEventListener('click', function () {
      var t = b.parentNode.querySelector('.tbd-track');
      t.scrollBy({ left: b.dataset.dir * t.clientWidth, behavior: 'smooth' });
    });\n  });\n</script>`
    : '';
  const font = meta.font_url
    ? `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>\n<link rel="stylesheet" href="${esc(meta.font_url)}">\n`
    : '';
  const note = data.sample ? '  <p class="tbd-note">Sample posts - set ACCESS_TOKEN in .env to show your gallery.</p>\n' : '';
  return `<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Social Widget</title>
${font}<style>
${css}</style>
</head>
<body>
<section class="tbd-widget tbd-t-${meta.slug} tbd-l-${meta.layout}${meta.clamp ? ' tbd-clamp' : ''}">
  <h1 class="tbd-header">Social Widget</h1>
${note}${body}
</section>${script}
</body>
</html>
`;
}

// ---------- server ----------

const app = express();

app.get('/', async (req, res) => {
  try {
    res.type('html').send(renderPage(await getPosts()));
  } catch (err) {
    console.error('[social-widget]', err);
    res.type('html').send(renderPage({ posts: [] }));
  }
});

app.listen(PORT, () => {
  console.log(`Social Widget (${theme.meta.name}) on http://localhost:${PORT}`
    + (TOKEN ? '' : ' - showing sample posts until ACCESS_TOKEN is set'));
});

===== FILE: themes/classic-card.css =====
/* Classic Card theme - values from themes-lite.json. Change the look here. */
  :root {
    --tbd-bg: #E9EAF0;
    --tbd-surface: #ffffff;
    --tbd-text: #000000;
    --tbd-author: #000000;
    --tbd-font: "Open Sans", system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    --tbd-weight: 400;
    --tbd-size: 14px;
    --tbd-radius: 8px;
    --tbd-img-radius: 0px;
    --tbd-gap: 10px;
    --tbd-pad: 14px;
    --tbd-cols: 3;
    --tbd-align: left;
    --tbd-lines: 4;
  }

  * { box-sizing: border-box; }
  body { margin: 0; background: var(--tbd-bg); color: var(--tbd-text);
    font-family: var(--tbd-font); font-weight: var(--tbd-weight); font-size: var(--tbd-size); }
  .tbd-widget { max-width: 1200px; margin: 0 auto; padding: 32px 16px; }
  .tbd-header { margin: 0 0 24px; text-align: center; font-size: 1.9em; font-weight: 700; }
  /* Card: one markup for every theme; the theme block below orders and hides parts */
  .tbd-card { display: flex; flex-direction: column; background: var(--tbd-surface); color: inherit;
    text-decoration: none; border-radius: var(--tbd-radius); overflow: hidden; text-align: var(--tbd-align); }
  /* --tbd-ph: the sample file's tiny base64 thumbnail (preview only; live posts have none) */
  .tbd-media { position: relative; overflow: hidden; border-radius: var(--tbd-img-radius);
    aspect-ratio: var(--tbd-ar, auto);
    background: var(--tbd-ph, none) center / cover no-repeat, linear-gradient(135deg, #e6e8f2, #f3e6ee); }
  /* Fallback tile: chat preview panes block outside images, so show the network name */
  .tbd-media::after { content: attr(data-network); position: absolute; inset: 0; display: grid;
    place-items: center; font-size: .85em; letter-spacing: .06em; text-transform: uppercase; opacity: .45; }
  .tbd-media img, .tbd-media video { position: relative; z-index: 1; display: block; width: 100%; height: 100%; object-fit: cover; }
  /* Blocked image: cover the broken-image icon with the blurred thumbnail, if any */
  .tbd-media img::before { content: ""; position: absolute; inset: -16px;
    background: var(--tbd-ph, none) center / cover no-repeat; filter: blur(8px); }
  .tbd-play { position: absolute; z-index: 2; right: 10px; top: 10px; width: 28px; height: 28px;
    border-radius: 50%; background: rgba(0,0,0,.55); }
  .tbd-play::before { content: ""; position: absolute; left: 11px; top: 8px;
    border-left: 9px solid #fff; border-top: 6px solid transparent; border-bottom: 6px solid transparent; }
  .tbd-head { display: flex; align-items: center; gap: 10px; padding: var(--tbd-pad); }
  .tbd-avatar { flex: none; width: 36px; height: 36px; border-radius: 50%; object-fit: cover;
    background: #d9dbe3; display: grid; place-items: center; font-weight: 700; color: #555; }
  /* Blocked avatar: show the author's initial instead of a broken-image icon */
  img.tbd-avatar { position: relative; }
  img.tbd-avatar::before { content: attr(data-initial); position: absolute; inset: 0; display: grid;
    place-items: center; border-radius: 50%; background: #d9dbe3; }
  .tbd-who { flex: 1; min-width: 0; display: flex; flex-direction: column; text-align: left; }
  .tbd-author { color: var(--tbd-author); font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .tbd-date { font-size: .8em; opacity: .6; }
  .tbd-net { flex: none; width: 24px; height: 24px; border-radius: 50%; display: grid; place-items: center;
    background: #888; color: #fff; font: 700 11px/1 system-ui, sans-serif; }
  .tbd-net::before { content: attr(data-mark); }
  .tbd-net[data-net="instagram"] { background: linear-gradient(45deg, #f9ce34, #ee2a7b 55%, #6228d7); }
  .tbd-net[data-net="facebook"] { background: #1877f2; }
  .tbd-net[data-net="twitter"], .tbd-net[data-net="x"], .tbd-net[data-net="tiktok"] { background: #111; }
  .tbd-net[data-net="youtube"] { background: #ff0000; }
  .tbd-net[data-net="linkedin"] { background: #0a66c2; }
  .tbd-net[data-net="pinterest"] { background: #e60023; }
  .tbd-net[data-net="google"] { background: #4285f4; }
  .tbd-net[data-net="yelp"] { background: #d32323; }
  .tbd-net[data-net="tripadvisor"] { background: #34e0a1; color: #000; }
  .tbd-net[data-net="trustpilot"] { background: #00b67a; }
  .tbd-stars { padding: var(--tbd-pad) var(--tbd-pad) 0; text-align: center; color: #f5b50a; letter-spacing: 2px; }
  .tbd-text { margin: 0; padding: 0 var(--tbd-pad) var(--tbd-pad); line-height: 1.5; }
  .tbd-card > :first-child.tbd-text { padding-top: var(--tbd-pad); }
  .tbd-clamp .tbd-text { display: -webkit-box; -webkit-box-orient: vertical;
    -webkit-line-clamp: var(--tbd-lines); line-clamp: var(--tbd-lines); overflow: hidden; }
  /* Starters only: no posts yet, and the "sample posts" note */
  .tbd-empty, .tbd-note { text-align: center; opacity: .7; margin: 0 0 16px; }

  /* Grid: --tbd-cols columns, 2 on tablet, 1 on phone */
  .tbd-track { display: grid; gap: var(--tbd-gap); align-items: start;
    grid-template-columns: repeat(var(--tbd-cols), minmax(0, 1fr)); }
  @media (max-width: 900px) { .tbd-track { grid-template-columns: repeat(2, minmax(0, 1fr)); } }
  @media (max-width: 560px) { .tbd-track { grid-template-columns: 1fr; } }

  .tbd-card { box-shadow: 0 2px 10px rgba(0,0,0,.08); }

===== FILE: themes/classic-card.json =====
{
  "name": "Classic Card",
  "slug": "classic-card",
  "type": "social",
  "layout": "grid",
  "parts": [
    "head",
    "text",
    "media"
  ],
  "clamp": true,
  "font_url": "https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;600;700&display=swap"
}

===== FILE: themes/classic-photo.css =====
/* Classic Photo theme - values from themes-lite.json. Change the look here. */
  :root {
    --tbd-bg: #E9EAF0;
    --tbd-surface: #ffffff;
    --tbd-text: #000000;
    --tbd-author: #000000;
    --tbd-font: system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    --tbd-weight: 400;
    --tbd-size: 14px;
    --tbd-radius: 8px;
    --tbd-img-radius: 8px;
    --tbd-gap: 12px;
    --tbd-pad: 16px;
    --tbd-cols: 3;
    --tbd-align: left;
    --tbd-lines: none;
  }

  * { box-sizing: border-box; }
  body { margin: 0; background: var(--tbd-bg); color: var(--tbd-text);
    font-family: var(--tbd-font); font-weight: var(--tbd-weight); font-size: var(--tbd-size); }
  .tbd-widget { max-width: 1200px; margin: 0 auto; padding: 32px 16px; }
  .tbd-header { margin: 0 0 24px; text-align: center; font-size: 1.9em; font-weight: 700; }
  /* Card: one markup for every theme; the theme block below orders and hides parts */
  .tbd-card { display: flex; flex-direction: column; background: var(--tbd-surface); color: inherit;
    text-decoration: none; border-radius: var(--tbd-radius); overflow: hidden; text-align: var(--tbd-align); }
  /* --tbd-ph: the sample file's tiny base64 thumbnail (preview only; live posts have none) */
  .tbd-media { position: relative; overflow: hidden; border-radius: var(--tbd-img-radius);
    aspect-ratio: var(--tbd-ar, auto);
    background: var(--tbd-ph, none) center / cover no-repeat, linear-gradient(135deg, #e6e8f2, #f3e6ee); }
  /* Fallback tile: chat preview panes block outside images, so show the network name */
  .tbd-media::after { content: attr(data-network); position: absolute; inset: 0; display: grid;
    place-items: center; font-size: .85em; letter-spacing: .06em; text-transform: uppercase; opacity: .45; }
  .tbd-media img, .tbd-media video { position: relative; z-index: 1; display: block; width: 100%; height: 100%; object-fit: cover; }
  /* Blocked image: cover the broken-image icon with the blurred thumbnail, if any */
  .tbd-media img::before { content: ""; position: absolute; inset: -16px;
    background: var(--tbd-ph, none) center / cover no-repeat; filter: blur(8px); }
  .tbd-play { position: absolute; z-index: 2; right: 10px; top: 10px; width: 28px; height: 28px;
    border-radius: 50%; background: rgba(0,0,0,.55); }
  .tbd-play::before { content: ""; position: absolute; left: 11px; top: 8px;
    border-left: 9px solid #fff; border-top: 6px solid transparent; border-bottom: 6px solid transparent; }
  .tbd-head { display: flex; align-items: center; gap: 10px; padding: var(--tbd-pad); }
  .tbd-avatar { flex: none; width: 36px; height: 36px; border-radius: 50%; object-fit: cover;
    background: #d9dbe3; display: grid; place-items: center; font-weight: 700; color: #555; }
  /* Blocked avatar: show the author's initial instead of a broken-image icon */
  img.tbd-avatar { position: relative; }
  img.tbd-avatar::before { content: attr(data-initial); position: absolute; inset: 0; display: grid;
    place-items: center; border-radius: 50%; background: #d9dbe3; }
  .tbd-who { flex: 1; min-width: 0; display: flex; flex-direction: column; text-align: left; }
  .tbd-author { color: var(--tbd-author); font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .tbd-date { font-size: .8em; opacity: .6; }
  .tbd-net { flex: none; width: 24px; height: 24px; border-radius: 50%; display: grid; place-items: center;
    background: #888; color: #fff; font: 700 11px/1 system-ui, sans-serif; }
  .tbd-net::before { content: attr(data-mark); }
  .tbd-net[data-net="instagram"] { background: linear-gradient(45deg, #f9ce34, #ee2a7b 55%, #6228d7); }
  .tbd-net[data-net="facebook"] { background: #1877f2; }
  .tbd-net[data-net="twitter"], .tbd-net[data-net="x"], .tbd-net[data-net="tiktok"] { background: #111; }
  .tbd-net[data-net="youtube"] { background: #ff0000; }
  .tbd-net[data-net="linkedin"] { background: #0a66c2; }
  .tbd-net[data-net="pinterest"] { background: #e60023; }
  .tbd-net[data-net="google"] { background: #4285f4; }
  .tbd-net[data-net="yelp"] { background: #d32323; }
  .tbd-net[data-net="tripadvisor"] { background: #34e0a1; color: #000; }
  .tbd-net[data-net="trustpilot"] { background: #00b67a; }
  .tbd-stars { padding: var(--tbd-pad) var(--tbd-pad) 0; text-align: center; color: #f5b50a; letter-spacing: 2px; }
  .tbd-text { margin: 0; padding: 0 var(--tbd-pad) var(--tbd-pad); line-height: 1.5; }
  .tbd-card > :first-child.tbd-text { padding-top: var(--tbd-pad); }
  .tbd-clamp .tbd-text { display: -webkit-box; -webkit-box-orient: vertical;
    -webkit-line-clamp: var(--tbd-lines); line-clamp: var(--tbd-lines); overflow: hidden; }
  /* Starters only: no posts yet, and the "sample posts" note */
  .tbd-empty, .tbd-note { text-align: center; opacity: .7; margin: 0 0 16px; }

  /* Grid: --tbd-cols columns, 2 on tablet, 1 on phone */
  .tbd-track { display: grid; gap: var(--tbd-gap); align-items: start;
    grid-template-columns: repeat(var(--tbd-cols), minmax(0, 1fr)); }
  @media (max-width: 900px) { .tbd-track { grid-template-columns: repeat(2, minmax(0, 1fr)); } }
  @media (max-width: 560px) { .tbd-track { grid-template-columns: 1fr; } }

  .tbd-card { box-shadow: 0 2px 10px rgba(0,0,0,.1); }
  .tbd-media { aspect-ratio: 16 / 9; }

===== FILE: themes/classic-photo.json =====
{
  "name": "Classic Photo",
  "slug": "classic-photo",
  "type": "social",
  "layout": "grid",
  "parts": [
    "media",
    "head"
  ],
  "clamp": false,
  "font_url": ""
}

===== FILE: themes/collage.css =====
/* Collage theme - values from themes-lite.json. Change the look here. */
  :root {
    --tbd-bg: #E9EAF0;
    --tbd-surface: #ffffff;
    --tbd-text: #000000;
    --tbd-author: #000000;
    --tbd-font: "Open Sans", system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    --tbd-weight: 400;
    --tbd-size: 14px;
    --tbd-radius: 8px;
    --tbd-img-radius: 0px;
    --tbd-gap: 10px;
    --tbd-pad: 14px;
    --tbd-cols: 3;
    --tbd-align: left;
    --tbd-lines: none;
  }

  * { box-sizing: border-box; }
  body { margin: 0; background: var(--tbd-bg); color: var(--tbd-text);
    font-family: var(--tbd-font); font-weight: var(--tbd-weight); font-size: var(--tbd-size); }
  .tbd-widget { max-width: 1200px; margin: 0 auto; padding: 32px 16px; }
  .tbd-header { margin: 0 0 24px; text-align: center; font-size: 1.9em; font-weight: 700; }
  /* Card: one markup for every theme; the theme block below orders and hides parts */
  .tbd-card { display: flex; flex-direction: column; background: var(--tbd-surface); color: inherit;
    text-decoration: none; border-radius: var(--tbd-radius); overflow: hidden; text-align: var(--tbd-align); }
  /* --tbd-ph: the sample file's tiny base64 thumbnail (preview only; live posts have none) */
  .tbd-media { position: relative; overflow: hidden; border-radius: var(--tbd-img-radius);
    aspect-ratio: var(--tbd-ar, auto);
    background: var(--tbd-ph, none) center / cover no-repeat, linear-gradient(135deg, #e6e8f2, #f3e6ee); }
  /* Fallback tile: chat preview panes block outside images, so show the network name */
  .tbd-media::after { content: attr(data-network); position: absolute; inset: 0; display: grid;
    place-items: center; font-size: .85em; letter-spacing: .06em; text-transform: uppercase; opacity: .45; }
  .tbd-media img, .tbd-media video { position: relative; z-index: 1; display: block; width: 100%; height: 100%; object-fit: cover; }
  /* Blocked image: cover the broken-image icon with the blurred thumbnail, if any */
  .tbd-media img::before { content: ""; position: absolute; inset: -16px;
    background: var(--tbd-ph, none) center / cover no-repeat; filter: blur(8px); }
  .tbd-play { position: absolute; z-index: 2; right: 10px; top: 10px; width: 28px; height: 28px;
    border-radius: 50%; background: rgba(0,0,0,.55); }
  .tbd-play::before { content: ""; position: absolute; left: 11px; top: 8px;
    border-left: 9px solid #fff; border-top: 6px solid transparent; border-bottom: 6px solid transparent; }
  .tbd-head { display: flex; align-items: center; gap: 10px; padding: var(--tbd-pad); }
  .tbd-avatar { flex: none; width: 36px; height: 36px; border-radius: 50%; object-fit: cover;
    background: #d9dbe3; display: grid; place-items: center; font-weight: 700; color: #555; }
  /* Blocked avatar: show the author's initial instead of a broken-image icon */
  img.tbd-avatar { position: relative; }
  img.tbd-avatar::before { content: attr(data-initial); position: absolute; inset: 0; display: grid;
    place-items: center; border-radius: 50%; background: #d9dbe3; }
  .tbd-who { flex: 1; min-width: 0; display: flex; flex-direction: column; text-align: left; }
  .tbd-author { color: var(--tbd-author); font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .tbd-date { font-size: .8em; opacity: .6; }
  .tbd-net { flex: none; width: 24px; height: 24px; border-radius: 50%; display: grid; place-items: center;
    background: #888; color: #fff; font: 700 11px/1 system-ui, sans-serif; }
  .tbd-net::before { content: attr(data-mark); }
  .tbd-net[data-net="instagram"] { background: linear-gradient(45deg, #f9ce34, #ee2a7b 55%, #6228d7); }
  .tbd-net[data-net="facebook"] { background: #1877f2; }
  .tbd-net[data-net="twitter"], .tbd-net[data-net="x"], .tbd-net[data-net="tiktok"] { background: #111; }
  .tbd-net[data-net="youtube"] { background: #ff0000; }
  .tbd-net[data-net="linkedin"] { background: #0a66c2; }
  .tbd-net[data-net="pinterest"] { background: #e60023; }
  .tbd-net[data-net="google"] { background: #4285f4; }
  .tbd-net[data-net="yelp"] { background: #d32323; }
  .tbd-net[data-net="tripadvisor"] { background: #34e0a1; color: #000; }
  .tbd-net[data-net="trustpilot"] { background: #00b67a; }
  .tbd-stars { padding: var(--tbd-pad) var(--tbd-pad) 0; text-align: center; color: #f5b50a; letter-spacing: 2px; }
  .tbd-text { margin: 0; padding: 0 var(--tbd-pad) var(--tbd-pad); line-height: 1.5; }
  .tbd-card > :first-child.tbd-text { padding-top: var(--tbd-pad); }
  .tbd-clamp .tbd-text { display: -webkit-box; -webkit-box-orient: vertical;
    -webkit-line-clamp: var(--tbd-lines); line-clamp: var(--tbd-lines); overflow: hidden; }
  /* Starters only: no posts yet, and the "sample posts" note */
  .tbd-empty, .tbd-note { text-align: center; opacity: .7; margin: 0 0 16px; }

  /* Collage: 3 columns of square tiles, the 1st of every 3 is a 2x2 feature tile */
  .tbd-track { display: grid; gap: var(--tbd-gap); grid-template-columns: repeat(3, minmax(0, 1fr));
    grid-auto-rows: 1fr; grid-auto-flow: dense; max-width: 900px; margin: 0 auto; }
  .tbd-card { position: relative; aspect-ratio: 1 / 1; }
  .tbd-card:nth-child(3n + 1) { grid-column: span 2; grid-row: span 2; }
  .tbd-card:nth-child(6n + 4) { grid-column: 2 / span 2; }
  .tbd-card .tbd-media { position: absolute; inset: 0; }
  .tbd-card .tbd-text { display: none; }
  .tbd-card--text .tbd-text { display: -webkit-box; padding: 16px; }
  .tbd-card--text { justify-content: center; background: linear-gradient(135deg, #fbe3c4, #f6c1d3); }
  /* Hover: darken, network badge and a "View post" button */
  .tbd-card::after { content: "View post"; position: absolute; z-index: 3; left: 50%; top: 58%;
    transform: translateX(-50%); padding: 8px 18px; border: 2px solid #fff; color: #fff;
    background: rgba(0,0,0,.45); opacity: 0; transition: opacity .2s; }
  .tbd-card:hover::after, .tbd-card:focus-visible::after { opacity: 1; }
  /* Phone: 2 columns, only the very first tile is big */
  @media (max-width: 700px) { .tbd-track { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .tbd-card:nth-child(n) { grid-column: auto; grid-row: auto; }
    .tbd-card:first-child { grid-column: span 2; grid-row: span 2; } }

===== FILE: themes/collage.json =====
{
  "name": "Collage",
  "slug": "collage",
  "type": "social",
  "layout": "collage",
  "parts": [
    "media",
    "text"
  ],
  "clamp": false,
  "font_url": "https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;600;700&display=swap"
}

===== FILE: themes/horizontal-columns.css =====
/* Horizontal Columns theme - values from themes-lite.json. Change the look here. */
  :root {
    --tbd-bg: #E9EAF0;
    --tbd-surface: #ffffff;
    --tbd-text: #000000;
    --tbd-author: #000000;
    --tbd-font: "Open Sans", system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    --tbd-weight: 400;
    --tbd-size: 14px;
    --tbd-radius: 8px;
    --tbd-img-radius: 0px;
    --tbd-gap: 10px;
    --tbd-pad: 14px;
    --tbd-cols: 3;
    --tbd-align: left;
    --tbd-lines: 5;
  }

  * { box-sizing: border-box; }
  body { margin: 0; background: var(--tbd-bg); color: var(--tbd-text);
    font-family: var(--tbd-font); font-weight: var(--tbd-weight); font-size: var(--tbd-size); }
  .tbd-widget { max-width: 1200px; margin: 0 auto; padding: 32px 16px; }
  .tbd-header { margin: 0 0 24px; text-align: center; font-size: 1.9em; font-weight: 700; }
  /* Card: one markup for every theme; the theme block below orders and hides parts */
  .tbd-card { display: flex; flex-direction: column; background: var(--tbd-surface); color: inherit;
    text-decoration: none; border-radius: var(--tbd-radius); overflow: hidden; text-align: var(--tbd-align); }
  /* --tbd-ph: the sample file's tiny base64 thumbnail (preview only; live posts have none) */
  .tbd-media { position: relative; overflow: hidden; border-radius: var(--tbd-img-radius);
    aspect-ratio: var(--tbd-ar, auto);
    background: var(--tbd-ph, none) center / cover no-repeat, linear-gradient(135deg, #e6e8f2, #f3e6ee); }
  /* Fallback tile: chat preview panes block outside images, so show the network name */
  .tbd-media::after { content: attr(data-network); position: absolute; inset: 0; display: grid;
    place-items: center; font-size: .85em; letter-spacing: .06em; text-transform: uppercase; opacity: .45; }
  .tbd-media img, .tbd-media video { position: relative; z-index: 1; display: block; width: 100%; height: 100%; object-fit: cover; }
  /* Blocked image: cover the broken-image icon with the blurred thumbnail, if any */
  .tbd-media img::before { content: ""; position: absolute; inset: -16px;
    background: var(--tbd-ph, none) center / cover no-repeat; filter: blur(8px); }
  .tbd-play { position: absolute; z-index: 2; right: 10px; top: 10px; width: 28px; height: 28px;
    border-radius: 50%; background: rgba(0,0,0,.55); }
  .tbd-play::before { content: ""; position: absolute; left: 11px; top: 8px;
    border-left: 9px solid #fff; border-top: 6px solid transparent; border-bottom: 6px solid transparent; }
  .tbd-head { display: flex; align-items: center; gap: 10px; padding: var(--tbd-pad); }
  .tbd-avatar { flex: none; width: 36px; height: 36px; border-radius: 50%; object-fit: cover;
    background: #d9dbe3; display: grid; place-items: center; font-weight: 700; color: #555; }
  /* Blocked avatar: show the author's initial instead of a broken-image icon */
  img.tbd-avatar { position: relative; }
  img.tbd-avatar::before { content: attr(data-initial); position: absolute; inset: 0; display: grid;
    place-items: center; border-radius: 50%; background: #d9dbe3; }
  .tbd-who { flex: 1; min-width: 0; display: flex; flex-direction: column; text-align: left; }
  .tbd-author { color: var(--tbd-author); font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .tbd-date { font-size: .8em; opacity: .6; }
  .tbd-net { flex: none; width: 24px; height: 24px; border-radius: 50%; display: grid; place-items: center;
    background: #888; color: #fff; font: 700 11px/1 system-ui, sans-serif; }
  .tbd-net::before { content: attr(data-mark); }
  .tbd-net[data-net="instagram"] { background: linear-gradient(45deg, #f9ce34, #ee2a7b 55%, #6228d7); }
  .tbd-net[data-net="facebook"] { background: #1877f2; }
  .tbd-net[data-net="twitter"], .tbd-net[data-net="x"], .tbd-net[data-net="tiktok"] { background: #111; }
  .tbd-net[data-net="youtube"] { background: #ff0000; }
  .tbd-net[data-net="linkedin"] { background: #0a66c2; }
  .tbd-net[data-net="pinterest"] { background: #e60023; }
  .tbd-net[data-net="google"] { background: #4285f4; }
  .tbd-net[data-net="yelp"] { background: #d32323; }
  .tbd-net[data-net="tripadvisor"] { background: #34e0a1; color: #000; }
  .tbd-net[data-net="trustpilot"] { background: #00b67a; }
  .tbd-stars { padding: var(--tbd-pad) var(--tbd-pad) 0; text-align: center; color: #f5b50a; letter-spacing: 2px; }
  .tbd-text { margin: 0; padding: 0 var(--tbd-pad) var(--tbd-pad); line-height: 1.5; }
  .tbd-card > :first-child.tbd-text { padding-top: var(--tbd-pad); }
  .tbd-clamp .tbd-text { display: -webkit-box; -webkit-box-orient: vertical;
    -webkit-line-clamp: var(--tbd-lines); line-clamp: var(--tbd-lines); overflow: hidden; }
  /* Starters only: no posts yet, and the "sample posts" note */
  .tbd-empty, .tbd-note { text-align: center; opacity: .7; margin: 0 0 16px; }

  /* Slider: one row that scrolls sideways; the arrows scroll one view */
  .tbd-slider { position: relative; }
  .tbd-track { display: flex; gap: var(--tbd-gap); overflow-x: auto; scroll-snap-type: x mandatory;
    scrollbar-width: none; padding: 4px 2px 12px; }
  .tbd-track::-webkit-scrollbar { display: none; }
  .tbd-card { flex: 0 0 calc((100% - (var(--tbd-cols) - 1) * var(--tbd-gap)) / var(--tbd-cols));
    scroll-snap-align: start; }
  @media (max-width: 900px) { .tbd-card { flex-basis: calc((100% - var(--tbd-gap)) / 2); } }
  @media (max-width: 560px) { .tbd-card { flex-basis: 85%; } }
  .tbd-arrow { position: absolute; z-index: 4; top: 50%; transform: translateY(-50%); width: 36px; height: 36px;
    border: 0; border-radius: 50%; background: #fff; color: #333; font-size: 20px; cursor: pointer;
    box-shadow: 0 2px 8px rgba(0,0,0,.2); }
  .tbd-arrow--prev { left: -10px; }
  .tbd-arrow--next { right: -10px; }

  .tbd-card { box-shadow: 0 2px 10px rgba(0,0,0,.08); text-align: center; }
  .tbd-media { aspect-ratio: 4 / 3; }
  /* Avatar sits on the image edge, name and badge centred under it */
  .tbd-head { flex-direction: column; margin-top: -28px; position: relative; z-index: 2; gap: 6px; }
  .tbd-avatar { width: 48px; height: 48px; border: 3px solid #fff; }
  .tbd-who { text-align: center; align-items: center; }

===== FILE: themes/horizontal-columns.json =====
{
  "name": "Horizontal Columns",
  "slug": "horizontal-columns",
  "type": "social",
  "layout": "slider",
  "parts": [
    "media",
    "head",
    "text"
  ],
  "clamp": true,
  "font_url": "https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;600;700&display=swap"
}

===== FILE: themes/horizontal-slider.css =====
/* Horizontal Slider theme - values from themes-lite.json. Change the look here. */
  :root {
    --tbd-bg: #E9EAF0;
    --tbd-surface: #ffffff;
    --tbd-text: #000000;
    --tbd-author: #000000;
    --tbd-font: "Open Sans", system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    --tbd-weight: 400;
    --tbd-size: 14px;
    --tbd-radius: 8px;
    --tbd-img-radius: 8px;
    --tbd-gap: 10px;
    --tbd-pad: 14px;
    --tbd-cols: 3;
    --tbd-align: left;
    --tbd-lines: 3;
  }

  * { box-sizing: border-box; }
  body { margin: 0; background: var(--tbd-bg); color: var(--tbd-text);
    font-family: var(--tbd-font); font-weight: var(--tbd-weight); font-size: var(--tbd-size); }
  .tbd-widget { max-width: 1200px; margin: 0 auto; padding: 32px 16px; }
  .tbd-header { margin: 0 0 24px; text-align: center; font-size: 1.9em; font-weight: 700; }
  /* Card: one markup for every theme; the theme block below orders and hides parts */
  .tbd-card { display: flex; flex-direction: column; background: var(--tbd-surface); color: inherit;
    text-decoration: none; border-radius: var(--tbd-radius); overflow: hidden; text-align: var(--tbd-align); }
  /* --tbd-ph: the sample file's tiny base64 thumbnail (preview only; live posts have none) */
  .tbd-media { position: relative; overflow: hidden; border-radius: var(--tbd-img-radius);
    aspect-ratio: var(--tbd-ar, auto);
    background: var(--tbd-ph, none) center / cover no-repeat, linear-gradient(135deg, #e6e8f2, #f3e6ee); }
  /* Fallback tile: chat preview panes block outside images, so show the network name */
  .tbd-media::after { content: attr(data-network); position: absolute; inset: 0; display: grid;
    place-items: center; font-size: .85em; letter-spacing: .06em; text-transform: uppercase; opacity: .45; }
  .tbd-media img, .tbd-media video { position: relative; z-index: 1; display: block; width: 100%; height: 100%; object-fit: cover; }
  /* Blocked image: cover the broken-image icon with the blurred thumbnail, if any */
  .tbd-media img::before { content: ""; position: absolute; inset: -16px;
    background: var(--tbd-ph, none) center / cover no-repeat; filter: blur(8px); }
  .tbd-play { position: absolute; z-index: 2; right: 10px; top: 10px; width: 28px; height: 28px;
    border-radius: 50%; background: rgba(0,0,0,.55); }
  .tbd-play::before { content: ""; position: absolute; left: 11px; top: 8px;
    border-left: 9px solid #fff; border-top: 6px solid transparent; border-bottom: 6px solid transparent; }
  .tbd-head { display: flex; align-items: center; gap: 10px; padding: var(--tbd-pad); }
  .tbd-avatar { flex: none; width: 36px; height: 36px; border-radius: 50%; object-fit: cover;
    background: #d9dbe3; display: grid; place-items: center; font-weight: 700; color: #555; }
  /* Blocked avatar: show the author's initial instead of a broken-image icon */
  img.tbd-avatar { position: relative; }
  img.tbd-avatar::before { content: attr(data-initial); position: absolute; inset: 0; display: grid;
    place-items: center; border-radius: 50%; background: #d9dbe3; }
  .tbd-who { flex: 1; min-width: 0; display: flex; flex-direction: column; text-align: left; }
  .tbd-author { color: var(--tbd-author); font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .tbd-date { font-size: .8em; opacity: .6; }
  .tbd-net { flex: none; width: 24px; height: 24px; border-radius: 50%; display: grid; place-items: center;
    background: #888; color: #fff; font: 700 11px/1 system-ui, sans-serif; }
  .tbd-net::before { content: attr(data-mark); }
  .tbd-net[data-net="instagram"] { background: linear-gradient(45deg, #f9ce34, #ee2a7b 55%, #6228d7); }
  .tbd-net[data-net="facebook"] { background: #1877f2; }
  .tbd-net[data-net="twitter"], .tbd-net[data-net="x"], .tbd-net[data-net="tiktok"] { background: #111; }
  .tbd-net[data-net="youtube"] { background: #ff0000; }
  .tbd-net[data-net="linkedin"] { background: #0a66c2; }
  .tbd-net[data-net="pinterest"] { background: #e60023; }
  .tbd-net[data-net="google"] { background: #4285f4; }
  .tbd-net[data-net="yelp"] { background: #d32323; }
  .tbd-net[data-net="tripadvisor"] { background: #34e0a1; color: #000; }
  .tbd-net[data-net="trustpilot"] { background: #00b67a; }
  .tbd-stars { padding: var(--tbd-pad) var(--tbd-pad) 0; text-align: center; color: #f5b50a; letter-spacing: 2px; }
  .tbd-text { margin: 0; padding: 0 var(--tbd-pad) var(--tbd-pad); line-height: 1.5; }
  .tbd-card > :first-child.tbd-text { padding-top: var(--tbd-pad); }
  .tbd-clamp .tbd-text { display: -webkit-box; -webkit-box-orient: vertical;
    -webkit-line-clamp: var(--tbd-lines); line-clamp: var(--tbd-lines); overflow: hidden; }
  /* Starters only: no posts yet, and the "sample posts" note */
  .tbd-empty, .tbd-note { text-align: center; opacity: .7; margin: 0 0 16px; }

  /* Slider: one row that scrolls sideways; the arrows scroll one view */
  .tbd-slider { position: relative; }
  .tbd-track { display: flex; gap: var(--tbd-gap); overflow-x: auto; scroll-snap-type: x mandatory;
    scrollbar-width: none; padding: 4px 2px 12px; }
  .tbd-track::-webkit-scrollbar { display: none; }
  .tbd-card { flex: 0 0 calc((100% - (var(--tbd-cols) - 1) * var(--tbd-gap)) / var(--tbd-cols));
    scroll-snap-align: start; }
  @media (max-width: 900px) { .tbd-card { flex-basis: calc((100% - var(--tbd-gap)) / 2); } }
  @media (max-width: 560px) { .tbd-card { flex-basis: 85%; } }
  .tbd-arrow { position: absolute; z-index: 4; top: 50%; transform: translateY(-50%); width: 36px; height: 36px;
    border: 0; border-radius: 50%; background: #fff; color: #333; font-size: 20px; cursor: pointer;
    box-shadow: 0 2px 8px rgba(0,0,0,.2); }
  .tbd-arrow--prev { left: -10px; }
  .tbd-arrow--next { right: -10px; }

  .tbd-card { border-radius: 0; background: none; }
  .tbd-media { aspect-ratio: 7 / 6; }
  .tbd-arrow { border-radius: 2px; background: rgba(0,0,0,.75); color: #fff; }
  .tbd-arrow--prev { left: 8px; } .tbd-arrow--next { right: 8px; }

===== FILE: themes/horizontal-slider.json =====
{
  "name": "Horizontal Slider",
  "slug": "horizontal-slider",
  "type": "social",
  "layout": "slider",
  "parts": [
    "media"
  ],
  "clamp": true,
  "font_url": "https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;600;700&display=swap"
}

===== FILE: themes/modern-card.css =====
/* Modern Card theme - values from themes-lite.json. Change the look here. */
  :root {
    --tbd-bg: #E9EAF0;
    --tbd-surface: #ffffff;
    --tbd-text: #2B2B2B;
    --tbd-author: #2B2B2B;
    --tbd-font: "Open Sans", system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    --tbd-weight: 300;
    --tbd-size: 14px;
    --tbd-radius: 8px;
    --tbd-img-radius: 0px;
    --tbd-gap: 10px;
    --tbd-pad: 12px;
    --tbd-cols: 3;
    --tbd-align: left;
    --tbd-lines: 3;
  }

  * { box-sizing: border-box; }
  body { margin: 0; background: var(--tbd-bg); color: var(--tbd-text);
    font-family: var(--tbd-font); font-weight: var(--tbd-weight); font-size: var(--tbd-size); }
  .tbd-widget { max-width: 1200px; margin: 0 auto; padding: 32px 16px; }
  .tbd-header { margin: 0 0 24px; text-align: center; font-size: 1.9em; font-weight: 700; }
  /* Card: one markup for every theme; the theme block below orders and hides parts */
  .tbd-card { display: flex; flex-direction: column; background: var(--tbd-surface); color: inherit;
    text-decoration: none; border-radius: var(--tbd-radius); overflow: hidden; text-align: var(--tbd-align); }
  /* --tbd-ph: the sample file's tiny base64 thumbnail (preview only; live posts have none) */
  .tbd-media { position: relative; overflow: hidden; border-radius: var(--tbd-img-radius);
    aspect-ratio: var(--tbd-ar, auto);
    background: var(--tbd-ph, none) center / cover no-repeat, linear-gradient(135deg, #e6e8f2, #f3e6ee); }
  /* Fallback tile: chat preview panes block outside images, so show the network name */
  .tbd-media::after { content: attr(data-network); position: absolute; inset: 0; display: grid;
    place-items: center; font-size: .85em; letter-spacing: .06em; text-transform: uppercase; opacity: .45; }
  .tbd-media img, .tbd-media video { position: relative; z-index: 1; display: block; width: 100%; height: 100%; object-fit: cover; }
  /* Blocked image: cover the broken-image icon with the blurred thumbnail, if any */
  .tbd-media img::before { content: ""; position: absolute; inset: -16px;
    background: var(--tbd-ph, none) center / cover no-repeat; filter: blur(8px); }
  .tbd-play { position: absolute; z-index: 2; right: 10px; top: 10px; width: 28px; height: 28px;
    border-radius: 50%; background: rgba(0,0,0,.55); }
  .tbd-play::before { content: ""; position: absolute; left: 11px; top: 8px;
    border-left: 9px solid #fff; border-top: 6px solid transparent; border-bottom: 6px solid transparent; }
  .tbd-head { display: flex; align-items: center; gap: 10px; padding: var(--tbd-pad); }
  .tbd-avatar { flex: none; width: 36px; height: 36px; border-radius: 50%; object-fit: cover;
    background: #d9dbe3; display: grid; place-items: center; font-weight: 700; color: #555; }
  /* Blocked avatar: show the author's initial instead of a broken-image icon */
  img.tbd-avatar { position: relative; }
  img.tbd-avatar::before { content: attr(data-initial); position: absolute; inset: 0; display: grid;
    place-items: center; border-radius: 50%; background: #d9dbe3; }
  .tbd-who { flex: 1; min-width: 0; display: flex; flex-direction: column; text-align: left; }
  .tbd-author { color: var(--tbd-author); font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .tbd-date { font-size: .8em; opacity: .6; }
  .tbd-net { flex: none; width: 24px; height: 24px; border-radius: 50%; display: grid; place-items: center;
    background: #888; color: #fff; font: 700 11px/1 system-ui, sans-serif; }
  .tbd-net::before { content: attr(data-mark); }
  .tbd-net[data-net="instagram"] { background: linear-gradient(45deg, #f9ce34, #ee2a7b 55%, #6228d7); }
  .tbd-net[data-net="facebook"] { background: #1877f2; }
  .tbd-net[data-net="twitter"], .tbd-net[data-net="x"], .tbd-net[data-net="tiktok"] { background: #111; }
  .tbd-net[data-net="youtube"] { background: #ff0000; }
  .tbd-net[data-net="linkedin"] { background: #0a66c2; }
  .tbd-net[data-net="pinterest"] { background: #e60023; }
  .tbd-net[data-net="google"] { background: #4285f4; }
  .tbd-net[data-net="yelp"] { background: #d32323; }
  .tbd-net[data-net="tripadvisor"] { background: #34e0a1; color: #000; }
  .tbd-net[data-net="trustpilot"] { background: #00b67a; }
  .tbd-stars { padding: var(--tbd-pad) var(--tbd-pad) 0; text-align: center; color: #f5b50a; letter-spacing: 2px; }
  .tbd-text { margin: 0; padding: 0 var(--tbd-pad) var(--tbd-pad); line-height: 1.5; }
  .tbd-card > :first-child.tbd-text { padding-top: var(--tbd-pad); }
  .tbd-clamp .tbd-text { display: -webkit-box; -webkit-box-orient: vertical;
    -webkit-line-clamp: var(--tbd-lines); line-clamp: var(--tbd-lines); overflow: hidden; }
  /* Starters only: no posts yet, and the "sample posts" note */
  .tbd-empty, .tbd-note { text-align: center; opacity: .7; margin: 0 0 16px; }

  /* Grid: --tbd-cols columns, 2 on tablet, 1 on phone */
  .tbd-track { display: grid; gap: var(--tbd-gap); align-items: start;
    grid-template-columns: repeat(var(--tbd-cols), minmax(0, 1fr)); }
  @media (max-width: 900px) { .tbd-track { grid-template-columns: repeat(2, minmax(0, 1fr)); } }
  @media (max-width: 560px) { .tbd-track { grid-template-columns: 1fr; } }

  .tbd-card { border: 1px solid #dcdde3; }
  .tbd-text { padding-top: var(--tbd-pad); }

===== FILE: themes/modern-card.json =====
{
  "name": "Modern Card",
  "slug": "modern-card",
  "type": "social",
  "layout": "grid",
  "parts": [
    "media",
    "text",
    "head"
  ],
  "clamp": true,
  "font_url": "https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;600;700&display=swap"
}

===== FILE: themes/reels.css =====
/* Reels theme - values from themes-lite.json. Change the look here. */
  :root {
    --tbd-bg: #ffffff;
    --tbd-surface: #ffffff;
    --tbd-text: #000000;
    --tbd-author: #2B2B2B;
    --tbd-font: "Open Sans", system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    --tbd-weight: regular;
    --tbd-size: 14px;
    --tbd-radius: 8px;
    --tbd-img-radius: 8px;
    --tbd-gap: 12px;
    --tbd-pad: 14px;
    --tbd-cols: 3;
    --tbd-align: left;
    --tbd-lines: none;
  }

  * { box-sizing: border-box; }
  body { margin: 0; background: var(--tbd-bg); color: var(--tbd-text);
    font-family: var(--tbd-font); font-weight: var(--tbd-weight); font-size: var(--tbd-size); }
  .tbd-widget { max-width: 1200px; margin: 0 auto; padding: 32px 16px; }
  .tbd-header { margin: 0 0 24px; text-align: center; font-size: 1.9em; font-weight: 700; }
  /* Card: one markup for every theme; the theme block below orders and hides parts */
  .tbd-card { display: flex; flex-direction: column; background: var(--tbd-surface); color: inherit;
    text-decoration: none; border-radius: var(--tbd-radius); overflow: hidden; text-align: var(--tbd-align); }
  /* --tbd-ph: the sample file's tiny base64 thumbnail (preview only; live posts have none) */
  .tbd-media { position: relative; overflow: hidden; border-radius: var(--tbd-img-radius);
    aspect-ratio: var(--tbd-ar, auto);
    background: var(--tbd-ph, none) center / cover no-repeat, linear-gradient(135deg, #e6e8f2, #f3e6ee); }
  /* Fallback tile: chat preview panes block outside images, so show the network name */
  .tbd-media::after { content: attr(data-network); position: absolute; inset: 0; display: grid;
    place-items: center; font-size: .85em; letter-spacing: .06em; text-transform: uppercase; opacity: .45; }
  .tbd-media img, .tbd-media video { position: relative; z-index: 1; display: block; width: 100%; height: 100%; object-fit: cover; }
  /* Blocked image: cover the broken-image icon with the blurred thumbnail, if any */
  .tbd-media img::before { content: ""; position: absolute; inset: -16px;
    background: var(--tbd-ph, none) center / cover no-repeat; filter: blur(8px); }
  .tbd-play { position: absolute; z-index: 2; right: 10px; top: 10px; width: 28px; height: 28px;
    border-radius: 50%; background: rgba(0,0,0,.55); }
  .tbd-play::before { content: ""; position: absolute; left: 11px; top: 8px;
    border-left: 9px solid #fff; border-top: 6px solid transparent; border-bottom: 6px solid transparent; }
  .tbd-head { display: flex; align-items: center; gap: 10px; padding: var(--tbd-pad); }
  .tbd-avatar { flex: none; width: 36px; height: 36px; border-radius: 50%; object-fit: cover;
    background: #d9dbe3; display: grid; place-items: center; font-weight: 700; color: #555; }
  /* Blocked avatar: show the author's initial instead of a broken-image icon */
  img.tbd-avatar { position: relative; }
  img.tbd-avatar::before { content: attr(data-initial); position: absolute; inset: 0; display: grid;
    place-items: center; border-radius: 50%; background: #d9dbe3; }
  .tbd-who { flex: 1; min-width: 0; display: flex; flex-direction: column; text-align: left; }
  .tbd-author { color: var(--tbd-author); font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .tbd-date { font-size: .8em; opacity: .6; }
  .tbd-net { flex: none; width: 24px; height: 24px; border-radius: 50%; display: grid; place-items: center;
    background: #888; color: #fff; font: 700 11px/1 system-ui, sans-serif; }
  .tbd-net::before { content: attr(data-mark); }
  .tbd-net[data-net="instagram"] { background: linear-gradient(45deg, #f9ce34, #ee2a7b 55%, #6228d7); }
  .tbd-net[data-net="facebook"] { background: #1877f2; }
  .tbd-net[data-net="twitter"], .tbd-net[data-net="x"], .tbd-net[data-net="tiktok"] { background: #111; }
  .tbd-net[data-net="youtube"] { background: #ff0000; }
  .tbd-net[data-net="linkedin"] { background: #0a66c2; }
  .tbd-net[data-net="pinterest"] { background: #e60023; }
  .tbd-net[data-net="google"] { background: #4285f4; }
  .tbd-net[data-net="yelp"] { background: #d32323; }
  .tbd-net[data-net="tripadvisor"] { background: #34e0a1; color: #000; }
  .tbd-net[data-net="trustpilot"] { background: #00b67a; }
  .tbd-stars { padding: var(--tbd-pad) var(--tbd-pad) 0; text-align: center; color: #f5b50a; letter-spacing: 2px; }
  .tbd-text { margin: 0; padding: 0 var(--tbd-pad) var(--tbd-pad); line-height: 1.5; }
  .tbd-card > :first-child.tbd-text { padding-top: var(--tbd-pad); }
  .tbd-clamp .tbd-text { display: -webkit-box; -webkit-box-orient: vertical;
    -webkit-line-clamp: var(--tbd-lines); line-clamp: var(--tbd-lines); overflow: hidden; }
  /* Starters only: no posts yet, and the "sample posts" note */
  .tbd-empty, .tbd-note { text-align: center; opacity: .7; margin: 0 0 16px; }

  /* Slider: one row that scrolls sideways; the arrows scroll one view */
  .tbd-slider { position: relative; }
  .tbd-track { display: flex; gap: var(--tbd-gap); overflow-x: auto; scroll-snap-type: x mandatory;
    scrollbar-width: none; padding: 4px 2px 12px; }
  .tbd-track::-webkit-scrollbar { display: none; }
  .tbd-card { flex: 0 0 calc((100% - (var(--tbd-cols) - 1) * var(--tbd-gap)) / var(--tbd-cols));
    scroll-snap-align: start; }
  @media (max-width: 900px) { .tbd-card { flex-basis: calc((100% - var(--tbd-gap)) / 2); } }
  @media (max-width: 560px) { .tbd-card { flex-basis: 85%; } }
  .tbd-arrow { position: absolute; z-index: 4; top: 50%; transform: translateY(-50%); width: 36px; height: 36px;
    border: 0; border-radius: 50%; background: #fff; color: #333; font-size: 20px; cursor: pointer;
    box-shadow: 0 2px 8px rgba(0,0,0,.2); }
  .tbd-arrow--prev { left: -10px; }
  .tbd-arrow--next { right: -10px; }

  .tbd-card { background: none; }
  .tbd-media { aspect-ratio: 9 / 16; }
  .tbd-arrow { display: none; }

===== FILE: themes/reels.json =====
{
  "name": "Reels",
  "slug": "reels",
  "type": "social",
  "layout": "slider",
  "parts": [
    "media"
  ],
  "clamp": false,
  "font_url": "https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;600;700&display=swap"
}

===== FILE: themes/review-box.css =====
/* Review Box theme - values from themes-lite.json. Change the look here. */
  :root {
    --tbd-bg: #ffffff;
    --tbd-surface: #fafafa;
    --tbd-text: #000000;
    --tbd-author: #000000;
    --tbd-font: system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    --tbd-weight: 400;
    --tbd-size: 14px;
    --tbd-radius: 3px;
    --tbd-img-radius: 0px;
    --tbd-gap: 10px;
    --tbd-pad: 12px;
    --tbd-cols: 2;
    --tbd-align: left;
    --tbd-lines: 5;
  }

  * { box-sizing: border-box; }
  body { margin: 0; background: var(--tbd-bg); color: var(--tbd-text);
    font-family: var(--tbd-font); font-weight: var(--tbd-weight); font-size: var(--tbd-size); }
  .tbd-widget { max-width: 1200px; margin: 0 auto; padding: 32px 16px; }
  .tbd-header { margin: 0 0 24px; text-align: center; font-size: 1.9em; font-weight: 700; }
  /* Card: one markup for every theme; the theme block below orders and hides parts */
  .tbd-card { display: flex; flex-direction: column; background: var(--tbd-surface); color: inherit;
    text-decoration: none; border-radius: var(--tbd-radius); overflow: hidden; text-align: var(--tbd-align); }
  /* --tbd-ph: the sample file's tiny base64 thumbnail (preview only; live posts have none) */
  .tbd-media { position: relative; overflow: hidden; border-radius: var(--tbd-img-radius);
    aspect-ratio: var(--tbd-ar, auto);
    background: var(--tbd-ph, none) center / cover no-repeat, linear-gradient(135deg, #e6e8f2, #f3e6ee); }
  /* Fallback tile: chat preview panes block outside images, so show the network name */
  .tbd-media::after { content: attr(data-network); position: absolute; inset: 0; display: grid;
    place-items: center; font-size: .85em; letter-spacing: .06em; text-transform: uppercase; opacity: .45; }
  .tbd-media img, .tbd-media video { position: relative; z-index: 1; display: block; width: 100%; height: 100%; object-fit: cover; }
  /* Blocked image: cover the broken-image icon with the blurred thumbnail, if any */
  .tbd-media img::before { content: ""; position: absolute; inset: -16px;
    background: var(--tbd-ph, none) center / cover no-repeat; filter: blur(8px); }
  .tbd-play { position: absolute; z-index: 2; right: 10px; top: 10px; width: 28px; height: 28px;
    border-radius: 50%; background: rgba(0,0,0,.55); }
  .tbd-play::before { content: ""; position: absolute; left: 11px; top: 8px;
    border-left: 9px solid #fff; border-top: 6px solid transparent; border-bottom: 6px solid transparent; }
  .tbd-head { display: flex; align-items: center; gap: 10px; padding: var(--tbd-pad); }
  .tbd-avatar { flex: none; width: 36px; height: 36px; border-radius: 50%; object-fit: cover;
    background: #d9dbe3; display: grid; place-items: center; font-weight: 700; color: #555; }
  /* Blocked avatar: show the author's initial instead of a broken-image icon */
  img.tbd-avatar { position: relative; }
  img.tbd-avatar::before { content: attr(data-initial); position: absolute; inset: 0; display: grid;
    place-items: center; border-radius: 50%; background: #d9dbe3; }
  .tbd-who { flex: 1; min-width: 0; display: flex; flex-direction: column; text-align: left; }
  .tbd-author { color: var(--tbd-author); font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .tbd-date { font-size: .8em; opacity: .6; }
  .tbd-net { flex: none; width: 24px; height: 24px; border-radius: 50%; display: grid; place-items: center;
    background: #888; color: #fff; font: 700 11px/1 system-ui, sans-serif; }
  .tbd-net::before { content: attr(data-mark); }
  .tbd-net[data-net="instagram"] { background: linear-gradient(45deg, #f9ce34, #ee2a7b 55%, #6228d7); }
  .tbd-net[data-net="facebook"] { background: #1877f2; }
  .tbd-net[data-net="twitter"], .tbd-net[data-net="x"], .tbd-net[data-net="tiktok"] { background: #111; }
  .tbd-net[data-net="youtube"] { background: #ff0000; }
  .tbd-net[data-net="linkedin"] { background: #0a66c2; }
  .tbd-net[data-net="pinterest"] { background: #e60023; }
  .tbd-net[data-net="google"] { background: #4285f4; }
  .tbd-net[data-net="yelp"] { background: #d32323; }
  .tbd-net[data-net="tripadvisor"] { background: #34e0a1; color: #000; }
  .tbd-net[data-net="trustpilot"] { background: #00b67a; }
  .tbd-stars { padding: var(--tbd-pad) var(--tbd-pad) 0; text-align: center; color: #f5b50a; letter-spacing: 2px; }
  .tbd-text { margin: 0; padding: 0 var(--tbd-pad) var(--tbd-pad); line-height: 1.5; }
  .tbd-card > :first-child.tbd-text { padding-top: var(--tbd-pad); }
  .tbd-clamp .tbd-text { display: -webkit-box; -webkit-box-orient: vertical;
    -webkit-line-clamp: var(--tbd-lines); line-clamp: var(--tbd-lines); overflow: hidden; }
  /* Starters only: no posts yet, and the "sample posts" note */
  .tbd-empty, .tbd-note { text-align: center; opacity: .7; margin: 0 0 16px; }

  /* Grid: --tbd-cols columns, 2 on tablet, 1 on phone */
  .tbd-track { display: grid; gap: var(--tbd-gap); align-items: start;
    grid-template-columns: repeat(var(--tbd-cols), minmax(0, 1fr)); }
  @media (max-width: 900px) { .tbd-track { grid-template-columns: repeat(2, minmax(0, 1fr)); } }
  @media (max-width: 560px) { .tbd-track { grid-template-columns: 1fr; } }

  .tbd-track { max-width: 760px; margin: 0 auto; }
  .tbd-card { box-shadow: 0 2px 12px rgba(0,0,0,.07); }
  .tbd-stars { padding-top: 16px; }
  .tbd-text { padding-top: 10px; }

===== FILE: themes/review-box.json =====
{
  "name": "Review Box",
  "slug": "review-box",
  "type": "review",
  "layout": "grid",
  "parts": [
    "stars",
    "text",
    "head"
  ],
  "clamp": true,
  "font_url": ""
}

===== FILE: themes/review-carousel.css =====
/* Review Carousel theme - values from themes-lite.json. Change the look here. */
  :root {
    --tbd-bg: #F0F7FF;
    --tbd-surface: #FFFFFF;
    --tbd-text: #000000;
    --tbd-author: #000000;
    --tbd-font: "Open Sans", system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    --tbd-weight: 400;
    --tbd-size: 14px;
    --tbd-radius: 8px;
    --tbd-img-radius: 8px;
    --tbd-gap: 12px;
    --tbd-pad: 16px;
    --tbd-cols: 3;
    --tbd-align: left;
    --tbd-lines: 6;
  }

  * { box-sizing: border-box; }
  body { margin: 0; background: var(--tbd-bg); color: var(--tbd-text);
    font-family: var(--tbd-font); font-weight: var(--tbd-weight); font-size: var(--tbd-size); }
  .tbd-widget { max-width: 1200px; margin: 0 auto; padding: 32px 16px; }
  .tbd-header { margin: 0 0 24px; text-align: center; font-size: 1.9em; font-weight: 700; }
  /* Card: one markup for every theme; the theme block below orders and hides parts */
  .tbd-card { display: flex; flex-direction: column; background: var(--tbd-surface); color: inherit;
    text-decoration: none; border-radius: var(--tbd-radius); overflow: hidden; text-align: var(--tbd-align); }
  /* --tbd-ph: the sample file's tiny base64 thumbnail (preview only; live posts have none) */
  .tbd-media { position: relative; overflow: hidden; border-radius: var(--tbd-img-radius);
    aspect-ratio: var(--tbd-ar, auto);
    background: var(--tbd-ph, none) center / cover no-repeat, linear-gradient(135deg, #e6e8f2, #f3e6ee); }
  /* Fallback tile: chat preview panes block outside images, so show the network name */
  .tbd-media::after { content: attr(data-network); position: absolute; inset: 0; display: grid;
    place-items: center; font-size: .85em; letter-spacing: .06em; text-transform: uppercase; opacity: .45; }
  .tbd-media img, .tbd-media video { position: relative; z-index: 1; display: block; width: 100%; height: 100%; object-fit: cover; }
  /* Blocked image: cover the broken-image icon with the blurred thumbnail, if any */
  .tbd-media img::before { content: ""; position: absolute; inset: -16px;
    background: var(--tbd-ph, none) center / cover no-repeat; filter: blur(8px); }
  .tbd-play { position: absolute; z-index: 2; right: 10px; top: 10px; width: 28px; height: 28px;
    border-radius: 50%; background: rgba(0,0,0,.55); }
  .tbd-play::before { content: ""; position: absolute; left: 11px; top: 8px;
    border-left: 9px solid #fff; border-top: 6px solid transparent; border-bottom: 6px solid transparent; }
  .tbd-head { display: flex; align-items: center; gap: 10px; padding: var(--tbd-pad); }
  .tbd-avatar { flex: none; width: 36px; height: 36px; border-radius: 50%; object-fit: cover;
    background: #d9dbe3; display: grid; place-items: center; font-weight: 700; color: #555; }
  /* Blocked avatar: show the author's initial instead of a broken-image icon */
  img.tbd-avatar { position: relative; }
  img.tbd-avatar::before { content: attr(data-initial); position: absolute; inset: 0; display: grid;
    place-items: center; border-radius: 50%; background: #d9dbe3; }
  .tbd-who { flex: 1; min-width: 0; display: flex; flex-direction: column; text-align: left; }
  .tbd-author { color: var(--tbd-author); font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .tbd-date { font-size: .8em; opacity: .6; }
  .tbd-net { flex: none; width: 24px; height: 24px; border-radius: 50%; display: grid; place-items: center;
    background: #888; color: #fff; font: 700 11px/1 system-ui, sans-serif; }
  .tbd-net::before { content: attr(data-mark); }
  .tbd-net[data-net="instagram"] { background: linear-gradient(45deg, #f9ce34, #ee2a7b 55%, #6228d7); }
  .tbd-net[data-net="facebook"] { background: #1877f2; }
  .tbd-net[data-net="twitter"], .tbd-net[data-net="x"], .tbd-net[data-net="tiktok"] { background: #111; }
  .tbd-net[data-net="youtube"] { background: #ff0000; }
  .tbd-net[data-net="linkedin"] { background: #0a66c2; }
  .tbd-net[data-net="pinterest"] { background: #e60023; }
  .tbd-net[data-net="google"] { background: #4285f4; }
  .tbd-net[data-net="yelp"] { background: #d32323; }
  .tbd-net[data-net="tripadvisor"] { background: #34e0a1; color: #000; }
  .tbd-net[data-net="trustpilot"] { background: #00b67a; }
  .tbd-stars { padding: var(--tbd-pad) var(--tbd-pad) 0; text-align: center; color: #f5b50a; letter-spacing: 2px; }
  .tbd-text { margin: 0; padding: 0 var(--tbd-pad) var(--tbd-pad); line-height: 1.5; }
  .tbd-card > :first-child.tbd-text { padding-top: var(--tbd-pad); }
  .tbd-clamp .tbd-text { display: -webkit-box; -webkit-box-orient: vertical;
    -webkit-line-clamp: var(--tbd-lines); line-clamp: var(--tbd-lines); overflow: hidden; }
  /* Starters only: no posts yet, and the "sample posts" note */
  .tbd-empty, .tbd-note { text-align: center; opacity: .7; margin: 0 0 16px; }

  /* Slider: one row that scrolls sideways; the arrows scroll one view */
  .tbd-slider { position: relative; }
  .tbd-track { display: flex; gap: var(--tbd-gap); overflow-x: auto; scroll-snap-type: x mandatory;
    scrollbar-width: none; padding: 4px 2px 12px; }
  .tbd-track::-webkit-scrollbar { display: none; }
  .tbd-card { flex: 0 0 calc((100% - (var(--tbd-cols) - 1) * var(--tbd-gap)) / var(--tbd-cols));
    scroll-snap-align: start; }
  @media (max-width: 900px) { .tbd-card { flex-basis: calc((100% - var(--tbd-gap)) / 2); } }
  @media (max-width: 560px) { .tbd-card { flex-basis: 85%; } }
  .tbd-arrow { position: absolute; z-index: 4; top: 50%; transform: translateY(-50%); width: 36px; height: 36px;
    border: 0; border-radius: 50%; background: #fff; color: #333; font-size: 20px; cursor: pointer;
    box-shadow: 0 2px 8px rgba(0,0,0,.2); }
  .tbd-arrow--prev { left: -10px; }
  .tbd-arrow--next { right: -10px; }

  .tbd-card { box-shadow: 0 2px 10px rgba(0,0,0,.08); }
  .tbd-stars { padding-top: 16px; }
  .tbd-text { padding-top: 10px; }

===== FILE: themes/review-carousel.json =====
{
  "name": "Review Carousel",
  "slug": "review-carousel",
  "type": "review",
  "layout": "slider",
  "parts": [
    "stars",
    "text",
    "head"
  ],
  "clamp": true,
  "font_url": "https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;600;700&display=swap"
}

===== FILE: themes/review-list.css =====
/* Review List theme - values from themes-lite.json. Change the look here. */
  :root {
    --tbd-bg: #ffffff;
    --tbd-surface: #FFFFFF;
    --tbd-text: #000000;
    --tbd-author: #000000;
    --tbd-font: "Open Sans", system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    --tbd-weight: 400;
    --tbd-size: 14px;
    --tbd-radius: 8px;
    --tbd-img-radius: 0px;
    --tbd-gap: 10px;
    --tbd-pad: 14px;
    --tbd-cols: 1;
    --tbd-align: left;
    --tbd-lines: 5;
  }

  * { box-sizing: border-box; }
  body { margin: 0; background: var(--tbd-bg); color: var(--tbd-text);
    font-family: var(--tbd-font); font-weight: var(--tbd-weight); font-size: var(--tbd-size); }
  .tbd-widget { max-width: 1200px; margin: 0 auto; padding: 32px 16px; }
  .tbd-header { margin: 0 0 24px; text-align: center; font-size: 1.9em; font-weight: 700; }
  /* Card: one markup for every theme; the theme block below orders and hides parts */
  .tbd-card { display: flex; flex-direction: column; background: var(--tbd-surface); color: inherit;
    text-decoration: none; border-radius: var(--tbd-radius); overflow: hidden; text-align: var(--tbd-align); }
  /* --tbd-ph: the sample file's tiny base64 thumbnail (preview only; live posts have none) */
  .tbd-media { position: relative; overflow: hidden; border-radius: var(--tbd-img-radius);
    aspect-ratio: var(--tbd-ar, auto);
    background: var(--tbd-ph, none) center / cover no-repeat, linear-gradient(135deg, #e6e8f2, #f3e6ee); }
  /* Fallback tile: chat preview panes block outside images, so show the network name */
  .tbd-media::after { content: attr(data-network); position: absolute; inset: 0; display: grid;
    place-items: center; font-size: .85em; letter-spacing: .06em; text-transform: uppercase; opacity: .45; }
  .tbd-media img, .tbd-media video { position: relative; z-index: 1; display: block; width: 100%; height: 100%; object-fit: cover; }
  /* Blocked image: cover the broken-image icon with the blurred thumbnail, if any */
  .tbd-media img::before { content: ""; position: absolute; inset: -16px;
    background: var(--tbd-ph, none) center / cover no-repeat; filter: blur(8px); }
  .tbd-play { position: absolute; z-index: 2; right: 10px; top: 10px; width: 28px; height: 28px;
    border-radius: 50%; background: rgba(0,0,0,.55); }
  .tbd-play::before { content: ""; position: absolute; left: 11px; top: 8px;
    border-left: 9px solid #fff; border-top: 6px solid transparent; border-bottom: 6px solid transparent; }
  .tbd-head { display: flex; align-items: center; gap: 10px; padding: var(--tbd-pad); }
  .tbd-avatar { flex: none; width: 36px; height: 36px; border-radius: 50%; object-fit: cover;
    background: #d9dbe3; display: grid; place-items: center; font-weight: 700; color: #555; }
  /* Blocked avatar: show the author's initial instead of a broken-image icon */
  img.tbd-avatar { position: relative; }
  img.tbd-avatar::before { content: attr(data-initial); position: absolute; inset: 0; display: grid;
    place-items: center; border-radius: 50%; background: #d9dbe3; }
  .tbd-who { flex: 1; min-width: 0; display: flex; flex-direction: column; text-align: left; }
  .tbd-author { color: var(--tbd-author); font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .tbd-date { font-size: .8em; opacity: .6; }
  .tbd-net { flex: none; width: 24px; height: 24px; border-radius: 50%; display: grid; place-items: center;
    background: #888; color: #fff; font: 700 11px/1 system-ui, sans-serif; }
  .tbd-net::before { content: attr(data-mark); }
  .tbd-net[data-net="instagram"] { background: linear-gradient(45deg, #f9ce34, #ee2a7b 55%, #6228d7); }
  .tbd-net[data-net="facebook"] { background: #1877f2; }
  .tbd-net[data-net="twitter"], .tbd-net[data-net="x"], .tbd-net[data-net="tiktok"] { background: #111; }
  .tbd-net[data-net="youtube"] { background: #ff0000; }
  .tbd-net[data-net="linkedin"] { background: #0a66c2; }
  .tbd-net[data-net="pinterest"] { background: #e60023; }
  .tbd-net[data-net="google"] { background: #4285f4; }
  .tbd-net[data-net="yelp"] { background: #d32323; }
  .tbd-net[data-net="tripadvisor"] { background: #34e0a1; color: #000; }
  .tbd-net[data-net="trustpilot"] { background: #00b67a; }
  .tbd-stars { padding: var(--tbd-pad) var(--tbd-pad) 0; text-align: center; color: #f5b50a; letter-spacing: 2px; }
  .tbd-text { margin: 0; padding: 0 var(--tbd-pad) var(--tbd-pad); line-height: 1.5; }
  .tbd-card > :first-child.tbd-text { padding-top: var(--tbd-pad); }
  .tbd-clamp .tbd-text { display: -webkit-box; -webkit-box-orient: vertical;
    -webkit-line-clamp: var(--tbd-lines); line-clamp: var(--tbd-lines); overflow: hidden; }
  /* Starters only: no posts yet, and the "sample posts" note */
  .tbd-empty, .tbd-note { text-align: center; opacity: .7; margin: 0 0 16px; }

  /* List: one column */
  .tbd-track { display: grid; gap: var(--tbd-gap); max-width: 640px; margin: 0 auto; }

  .tbd-card { box-shadow: 0 2px 12px rgba(0,0,0,.07); }
  .tbd-stars { padding-top: 0; }
  .tbd-text { padding-top: 8px; }

===== FILE: themes/review-list.json =====
{
  "name": "Review List",
  "slug": "review-list",
  "type": "review",
  "layout": "list",
  "parts": [
    "head",
    "stars",
    "text"
  ],
  "clamp": true,
  "font_url": "https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;600;700&display=swap"
}

===== FILE: themes/single-post.css =====
/* Single Post theme - values from themes-lite.json. Change the look here. */
  :root {
    --tbd-bg: #E9EAF0;
    --tbd-surface: #ffffff;
    --tbd-text: #2B2B2B;
    --tbd-author: #2B2B2B;
    --tbd-font: "Inter", system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    --tbd-weight: 400;
    --tbd-size: 14px;
    --tbd-radius: 3px;
    --tbd-img-radius: 0px;
    --tbd-gap: 10px;
    --tbd-pad: 12px;
    --tbd-cols: 1;
    --tbd-align: center;
    --tbd-lines: 5;
  }

  * { box-sizing: border-box; }
  body { margin: 0; background: var(--tbd-bg); color: var(--tbd-text);
    font-family: var(--tbd-font); font-weight: var(--tbd-weight); font-size: var(--tbd-size); }
  .tbd-widget { max-width: 1200px; margin: 0 auto; padding: 32px 16px; }
  .tbd-header { margin: 0 0 24px; text-align: center; font-size: 1.9em; font-weight: 700; }
  /* Card: one markup for every theme; the theme block below orders and hides parts */
  .tbd-card { display: flex; flex-direction: column; background: var(--tbd-surface); color: inherit;
    text-decoration: none; border-radius: var(--tbd-radius); overflow: hidden; text-align: var(--tbd-align); }
  /* --tbd-ph: the sample file's tiny base64 thumbnail (preview only; live posts have none) */
  .tbd-media { position: relative; overflow: hidden; border-radius: var(--tbd-img-radius);
    aspect-ratio: var(--tbd-ar, auto);
    background: var(--tbd-ph, none) center / cover no-repeat, linear-gradient(135deg, #e6e8f2, #f3e6ee); }
  /* Fallback tile: chat preview panes block outside images, so show the network name */
  .tbd-media::after { content: attr(data-network); position: absolute; inset: 0; display: grid;
    place-items: center; font-size: .85em; letter-spacing: .06em; text-transform: uppercase; opacity: .45; }
  .tbd-media img, .tbd-media video { position: relative; z-index: 1; display: block; width: 100%; height: 100%; object-fit: cover; }
  /* Blocked image: cover the broken-image icon with the blurred thumbnail, if any */
  .tbd-media img::before { content: ""; position: absolute; inset: -16px;
    background: var(--tbd-ph, none) center / cover no-repeat; filter: blur(8px); }
  .tbd-play { position: absolute; z-index: 2; right: 10px; top: 10px; width: 28px; height: 28px;
    border-radius: 50%; background: rgba(0,0,0,.55); }
  .tbd-play::before { content: ""; position: absolute; left: 11px; top: 8px;
    border-left: 9px solid #fff; border-top: 6px solid transparent; border-bottom: 6px solid transparent; }
  .tbd-head { display: flex; align-items: center; gap: 10px; padding: var(--tbd-pad); }
  .tbd-avatar { flex: none; width: 36px; height: 36px; border-radius: 50%; object-fit: cover;
    background: #d9dbe3; display: grid; place-items: center; font-weight: 700; color: #555; }
  /* Blocked avatar: show the author's initial instead of a broken-image icon */
  img.tbd-avatar { position: relative; }
  img.tbd-avatar::before { content: attr(data-initial); position: absolute; inset: 0; display: grid;
    place-items: center; border-radius: 50%; background: #d9dbe3; }
  .tbd-who { flex: 1; min-width: 0; display: flex; flex-direction: column; text-align: left; }
  .tbd-author { color: var(--tbd-author); font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .tbd-date { font-size: .8em; opacity: .6; }
  .tbd-net { flex: none; width: 24px; height: 24px; border-radius: 50%; display: grid; place-items: center;
    background: #888; color: #fff; font: 700 11px/1 system-ui, sans-serif; }
  .tbd-net::before { content: attr(data-mark); }
  .tbd-net[data-net="instagram"] { background: linear-gradient(45deg, #f9ce34, #ee2a7b 55%, #6228d7); }
  .tbd-net[data-net="facebook"] { background: #1877f2; }
  .tbd-net[data-net="twitter"], .tbd-net[data-net="x"], .tbd-net[data-net="tiktok"] { background: #111; }
  .tbd-net[data-net="youtube"] { background: #ff0000; }
  .tbd-net[data-net="linkedin"] { background: #0a66c2; }
  .tbd-net[data-net="pinterest"] { background: #e60023; }
  .tbd-net[data-net="google"] { background: #4285f4; }
  .tbd-net[data-net="yelp"] { background: #d32323; }
  .tbd-net[data-net="tripadvisor"] { background: #34e0a1; color: #000; }
  .tbd-net[data-net="trustpilot"] { background: #00b67a; }
  .tbd-stars { padding: var(--tbd-pad) var(--tbd-pad) 0; text-align: center; color: #f5b50a; letter-spacing: 2px; }
  .tbd-text { margin: 0; padding: 0 var(--tbd-pad) var(--tbd-pad); line-height: 1.5; }
  .tbd-card > :first-child.tbd-text { padding-top: var(--tbd-pad); }
  .tbd-clamp .tbd-text { display: -webkit-box; -webkit-box-orient: vertical;
    -webkit-line-clamp: var(--tbd-lines); line-clamp: var(--tbd-lines); overflow: hidden; }
  /* Starters only: no posts yet, and the "sample posts" note */
  .tbd-empty, .tbd-note { text-align: center; opacity: .7; margin: 0 0 16px; }

  /* Slider: one row that scrolls sideways; the arrows scroll one view */
  .tbd-slider { position: relative; }
  .tbd-track { display: flex; gap: var(--tbd-gap); overflow-x: auto; scroll-snap-type: x mandatory;
    scrollbar-width: none; padding: 4px 2px 12px; }
  .tbd-track::-webkit-scrollbar { display: none; }
  .tbd-card { flex: 0 0 calc((100% - (var(--tbd-cols) - 1) * var(--tbd-gap)) / var(--tbd-cols));
    scroll-snap-align: start; }
  @media (max-width: 900px) { .tbd-card { flex-basis: calc((100% - var(--tbd-gap)) / 2); } }
  @media (max-width: 560px) { .tbd-card { flex-basis: 85%; } }
  .tbd-arrow { position: absolute; z-index: 4; top: 50%; transform: translateY(-50%); width: 36px; height: 36px;
    border: 0; border-radius: 50%; background: #fff; color: #333; font-size: 20px; cursor: pointer;
    box-shadow: 0 2px 8px rgba(0,0,0,.2); }
  .tbd-arrow--prev { left: -10px; }
  .tbd-arrow--next { right: -10px; }

  .tbd-slider { max-width: 560px; margin: 0 auto; }
  .tbd-card { flex-basis: 100%; border-radius: 0; background: none; }
  .tbd-media { aspect-ratio: 1 / 1.07; }
  .tbd-arrow { width: 48px; height: 48px; background: rgba(255,255,255,.85); }
  .tbd-arrow--prev { left: 12px; } .tbd-arrow--next { right: 12px; }
  @media (max-width: 900px) { .tbd-card { flex-basis: 100%; } }

===== FILE: themes/single-post.json =====
{
  "name": "Single Post",
  "slug": "single-post",
  "type": "social",
  "layout": "slider",
  "parts": [
    "media"
  ],
  "clamp": true,
  "font_url": "https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap"
}

===== FILE: themes/slider.css =====
/* Slider theme - values from themes-lite.json. Change the look here. */
  :root {
    --tbd-bg: #ffffff;
    --tbd-surface: #fafafa;
    --tbd-text: #1f1f1f;
    --tbd-author: #000000;
    --tbd-font: system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    --tbd-weight: 400;
    --tbd-size: 14px;
    --tbd-radius: 3px;
    --tbd-img-radius: 8px;
    --tbd-gap: 10px;
    --tbd-pad: 12px;
    --tbd-cols: 4;
    --tbd-align: left;
    --tbd-lines: none;
  }

  * { box-sizing: border-box; }
  body { margin: 0; background: var(--tbd-bg); color: var(--tbd-text);
    font-family: var(--tbd-font); font-weight: var(--tbd-weight); font-size: var(--tbd-size); }
  .tbd-widget { max-width: 1200px; margin: 0 auto; padding: 32px 16px; }
  .tbd-header { margin: 0 0 24px; text-align: center; font-size: 1.9em; font-weight: 700; }
  /* Card: one markup for every theme; the theme block below orders and hides parts */
  .tbd-card { display: flex; flex-direction: column; background: var(--tbd-surface); color: inherit;
    text-decoration: none; border-radius: var(--tbd-radius); overflow: hidden; text-align: var(--tbd-align); }
  /* --tbd-ph: the sample file's tiny base64 thumbnail (preview only; live posts have none) */
  .tbd-media { position: relative; overflow: hidden; border-radius: var(--tbd-img-radius);
    aspect-ratio: var(--tbd-ar, auto);
    background: var(--tbd-ph, none) center / cover no-repeat, linear-gradient(135deg, #e6e8f2, #f3e6ee); }
  /* Fallback tile: chat preview panes block outside images, so show the network name */
  .tbd-media::after { content: attr(data-network); position: absolute; inset: 0; display: grid;
    place-items: center; font-size: .85em; letter-spacing: .06em; text-transform: uppercase; opacity: .45; }
  .tbd-media img, .tbd-media video { position: relative; z-index: 1; display: block; width: 100%; height: 100%; object-fit: cover; }
  /* Blocked image: cover the broken-image icon with the blurred thumbnail, if any */
  .tbd-media img::before { content: ""; position: absolute; inset: -16px;
    background: var(--tbd-ph, none) center / cover no-repeat; filter: blur(8px); }
  .tbd-play { position: absolute; z-index: 2; right: 10px; top: 10px; width: 28px; height: 28px;
    border-radius: 50%; background: rgba(0,0,0,.55); }
  .tbd-play::before { content: ""; position: absolute; left: 11px; top: 8px;
    border-left: 9px solid #fff; border-top: 6px solid transparent; border-bottom: 6px solid transparent; }
  .tbd-head { display: flex; align-items: center; gap: 10px; padding: var(--tbd-pad); }
  .tbd-avatar { flex: none; width: 36px; height: 36px; border-radius: 50%; object-fit: cover;
    background: #d9dbe3; display: grid; place-items: center; font-weight: 700; color: #555; }
  /* Blocked avatar: show the author's initial instead of a broken-image icon */
  img.tbd-avatar { position: relative; }
  img.tbd-avatar::before { content: attr(data-initial); position: absolute; inset: 0; display: grid;
    place-items: center; border-radius: 50%; background: #d9dbe3; }
  .tbd-who { flex: 1; min-width: 0; display: flex; flex-direction: column; text-align: left; }
  .tbd-author { color: var(--tbd-author); font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .tbd-date { font-size: .8em; opacity: .6; }
  .tbd-net { flex: none; width: 24px; height: 24px; border-radius: 50%; display: grid; place-items: center;
    background: #888; color: #fff; font: 700 11px/1 system-ui, sans-serif; }
  .tbd-net::before { content: attr(data-mark); }
  .tbd-net[data-net="instagram"] { background: linear-gradient(45deg, #f9ce34, #ee2a7b 55%, #6228d7); }
  .tbd-net[data-net="facebook"] { background: #1877f2; }
  .tbd-net[data-net="twitter"], .tbd-net[data-net="x"], .tbd-net[data-net="tiktok"] { background: #111; }
  .tbd-net[data-net="youtube"] { background: #ff0000; }
  .tbd-net[data-net="linkedin"] { background: #0a66c2; }
  .tbd-net[data-net="pinterest"] { background: #e60023; }
  .tbd-net[data-net="google"] { background: #4285f4; }
  .tbd-net[data-net="yelp"] { background: #d32323; }
  .tbd-net[data-net="tripadvisor"] { background: #34e0a1; color: #000; }
  .tbd-net[data-net="trustpilot"] { background: #00b67a; }
  .tbd-stars { padding: var(--tbd-pad) var(--tbd-pad) 0; text-align: center; color: #f5b50a; letter-spacing: 2px; }
  .tbd-text { margin: 0; padding: 0 var(--tbd-pad) var(--tbd-pad); line-height: 1.5; }
  .tbd-card > :first-child.tbd-text { padding-top: var(--tbd-pad); }
  .tbd-clamp .tbd-text { display: -webkit-box; -webkit-box-orient: vertical;
    -webkit-line-clamp: var(--tbd-lines); line-clamp: var(--tbd-lines); overflow: hidden; }
  /* Starters only: no posts yet, and the "sample posts" note */
  .tbd-empty, .tbd-note { text-align: center; opacity: .7; margin: 0 0 16px; }

  /* Slider: one row that scrolls sideways; the arrows scroll one view */
  .tbd-slider { position: relative; }
  .tbd-track { display: flex; gap: var(--tbd-gap); overflow-x: auto; scroll-snap-type: x mandatory;
    scrollbar-width: none; padding: 4px 2px 12px; }
  .tbd-track::-webkit-scrollbar { display: none; }
  .tbd-card { flex: 0 0 calc((100% - (var(--tbd-cols) - 1) * var(--tbd-gap)) / var(--tbd-cols));
    scroll-snap-align: start; }
  @media (max-width: 900px) { .tbd-card { flex-basis: calc((100% - var(--tbd-gap)) / 2); } }
  @media (max-width: 560px) { .tbd-card { flex-basis: 85%; } }
  .tbd-arrow { position: absolute; z-index: 4; top: 50%; transform: translateY(-50%); width: 36px; height: 36px;
    border: 0; border-radius: 50%; background: #fff; color: #333; font-size: 20px; cursor: pointer;
    box-shadow: 0 2px 8px rgba(0,0,0,.2); }
  .tbd-arrow--prev { left: -10px; }
  .tbd-arrow--next { right: -10px; }

  .tbd-card { background: none; }
  .tbd-media { aspect-ratio: 1 / 1; }
  /* Hover: darken the photo and show the network name */
  .tbd-media::before { content: attr(data-network); position: absolute; z-index: 2; inset: 0; display: grid;
    place-items: center; color: #fff; font-weight: 700; background: rgba(0,0,0,.4); opacity: 0; transition: opacity .2s; }
  .tbd-card:hover .tbd-media::before, .tbd-card:focus-visible .tbd-media::before { opacity: 1; }

===== FILE: themes/slider.json =====
{
  "name": "Slider",
  "slug": "slider",
  "type": "social",
  "layout": "slider",
  "parts": [
    "media"
  ],
  "clamp": false,
  "font_url": ""
}

===== FILE: themes/social-card.css =====
/* Social Card theme - values from themes-lite.json. Change the look here. */
  :root {
    --tbd-bg: #E9EAF0;
    --tbd-surface: #ffffff;
    --tbd-text: #000000;
    --tbd-author: #000000;
    --tbd-font: "Open Sans", system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    --tbd-weight: 400;
    --tbd-size: 14px;
    --tbd-radius: 8px;
    --tbd-img-radius: 0px;
    --tbd-gap: 10px;
    --tbd-pad: 14px;
    --tbd-cols: 3;
    --tbd-align: left;
    --tbd-lines: 5;
  }

  * { box-sizing: border-box; }
  body { margin: 0; background: var(--tbd-bg); color: var(--tbd-text);
    font-family: var(--tbd-font); font-weight: var(--tbd-weight); font-size: var(--tbd-size); }
  .tbd-widget { max-width: 1200px; margin: 0 auto; padding: 32px 16px; }
  .tbd-header { margin: 0 0 24px; text-align: center; font-size: 1.9em; font-weight: 700; }
  /* Card: one markup for every theme; the theme block below orders and hides parts */
  .tbd-card { display: flex; flex-direction: column; background: var(--tbd-surface); color: inherit;
    text-decoration: none; border-radius: var(--tbd-radius); overflow: hidden; text-align: var(--tbd-align); }
  /* --tbd-ph: the sample file's tiny base64 thumbnail (preview only; live posts have none) */
  .tbd-media { position: relative; overflow: hidden; border-radius: var(--tbd-img-radius);
    aspect-ratio: var(--tbd-ar, auto);
    background: var(--tbd-ph, none) center / cover no-repeat, linear-gradient(135deg, #e6e8f2, #f3e6ee); }
  /* Fallback tile: chat preview panes block outside images, so show the network name */
  .tbd-media::after { content: attr(data-network); position: absolute; inset: 0; display: grid;
    place-items: center; font-size: .85em; letter-spacing: .06em; text-transform: uppercase; opacity: .45; }
  .tbd-media img, .tbd-media video { position: relative; z-index: 1; display: block; width: 100%; height: 100%; object-fit: cover; }
  /* Blocked image: cover the broken-image icon with the blurred thumbnail, if any */
  .tbd-media img::before { content: ""; position: absolute; inset: -16px;
    background: var(--tbd-ph, none) center / cover no-repeat; filter: blur(8px); }
  .tbd-play { position: absolute; z-index: 2; right: 10px; top: 10px; width: 28px; height: 28px;
    border-radius: 50%; background: rgba(0,0,0,.55); }
  .tbd-play::before { content: ""; position: absolute; left: 11px; top: 8px;
    border-left: 9px solid #fff; border-top: 6px solid transparent; border-bottom: 6px solid transparent; }
  .tbd-head { display: flex; align-items: center; gap: 10px; padding: var(--tbd-pad); }
  .tbd-avatar { flex: none; width: 36px; height: 36px; border-radius: 50%; object-fit: cover;
    background: #d9dbe3; display: grid; place-items: center; font-weight: 700; color: #555; }
  /* Blocked avatar: show the author's initial instead of a broken-image icon */
  img.tbd-avatar { position: relative; }
  img.tbd-avatar::before { content: attr(data-initial); position: absolute; inset: 0; display: grid;
    place-items: center; border-radius: 50%; background: #d9dbe3; }
  .tbd-who { flex: 1; min-width: 0; display: flex; flex-direction: column; text-align: left; }
  .tbd-author { color: var(--tbd-author); font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .tbd-date { font-size: .8em; opacity: .6; }
  .tbd-net { flex: none; width: 24px; height: 24px; border-radius: 50%; display: grid; place-items: center;
    background: #888; color: #fff; font: 700 11px/1 system-ui, sans-serif; }
  .tbd-net::before { content: attr(data-mark); }
  .tbd-net[data-net="instagram"] { background: linear-gradient(45deg, #f9ce34, #ee2a7b 55%, #6228d7); }
  .tbd-net[data-net="facebook"] { background: #1877f2; }
  .tbd-net[data-net="twitter"], .tbd-net[data-net="x"], .tbd-net[data-net="tiktok"] { background: #111; }
  .tbd-net[data-net="youtube"] { background: #ff0000; }
  .tbd-net[data-net="linkedin"] { background: #0a66c2; }
  .tbd-net[data-net="pinterest"] { background: #e60023; }
  .tbd-net[data-net="google"] { background: #4285f4; }
  .tbd-net[data-net="yelp"] { background: #d32323; }
  .tbd-net[data-net="tripadvisor"] { background: #34e0a1; color: #000; }
  .tbd-net[data-net="trustpilot"] { background: #00b67a; }
  .tbd-stars { padding: var(--tbd-pad) var(--tbd-pad) 0; text-align: center; color: #f5b50a; letter-spacing: 2px; }
  .tbd-text { margin: 0; padding: 0 var(--tbd-pad) var(--tbd-pad); line-height: 1.5; }
  .tbd-card > :first-child.tbd-text { padding-top: var(--tbd-pad); }
  .tbd-clamp .tbd-text { display: -webkit-box; -webkit-box-orient: vertical;
    -webkit-line-clamp: var(--tbd-lines); line-clamp: var(--tbd-lines); overflow: hidden; }
  /* Starters only: no posts yet, and the "sample posts" note */
  .tbd-empty, .tbd-note { text-align: center; opacity: .7; margin: 0 0 16px; }

  /* Grid: --tbd-cols columns, 2 on tablet, 1 on phone */
  .tbd-track { display: grid; gap: var(--tbd-gap); align-items: start;
    grid-template-columns: repeat(var(--tbd-cols), minmax(0, 1fr)); }
  @media (max-width: 900px) { .tbd-track { grid-template-columns: repeat(2, minmax(0, 1fr)); } }
  @media (max-width: 560px) { .tbd-track { grid-template-columns: 1fr; } }

  .tbd-card { border: 1px solid #dcdde3; }

===== FILE: themes/social-card.json =====
{
  "name": "Social Card",
  "slug": "social-card",
  "type": "social",
  "layout": "grid",
  "parts": [
    "media",
    "head",
    "text"
  ],
  "clamp": true,
  "font_url": "https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;600;700&display=swap"
}

===== FILE: themes/square-photo.css =====
/* Square Photo theme - values from themes-lite.json. Change the look here. */
  :root {
    --tbd-bg: #E9EAF0;
    --tbd-surface: #ffffff;
    --tbd-text: #000000;
    --tbd-author: #000000;
    --tbd-font: "Open Sans", system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    --tbd-weight: 400;
    --tbd-size: 14px;
    --tbd-radius: 8px;
    --tbd-img-radius: 0px;
    --tbd-gap: 10px;
    --tbd-pad: 14px;
    --tbd-cols: 3;
    --tbd-align: left;
    --tbd-lines: none;
  }

  * { box-sizing: border-box; }
  body { margin: 0; background: var(--tbd-bg); color: var(--tbd-text);
    font-family: var(--tbd-font); font-weight: var(--tbd-weight); font-size: var(--tbd-size); }
  .tbd-widget { max-width: 1200px; margin: 0 auto; padding: 32px 16px; }
  .tbd-header { margin: 0 0 24px; text-align: center; font-size: 1.9em; font-weight: 700; }
  /* Card: one markup for every theme; the theme block below orders and hides parts */
  .tbd-card { display: flex; flex-direction: column; background: var(--tbd-surface); color: inherit;
    text-decoration: none; border-radius: var(--tbd-radius); overflow: hidden; text-align: var(--tbd-align); }
  /* --tbd-ph: the sample file's tiny base64 thumbnail (preview only; live posts have none) */
  .tbd-media { position: relative; overflow: hidden; border-radius: var(--tbd-img-radius);
    aspect-ratio: var(--tbd-ar, auto);
    background: var(--tbd-ph, none) center / cover no-repeat, linear-gradient(135deg, #e6e8f2, #f3e6ee); }
  /* Fallback tile: chat preview panes block outside images, so show the network name */
  .tbd-media::after { content: attr(data-network); position: absolute; inset: 0; display: grid;
    place-items: center; font-size: .85em; letter-spacing: .06em; text-transform: uppercase; opacity: .45; }
  .tbd-media img, .tbd-media video { position: relative; z-index: 1; display: block; width: 100%; height: 100%; object-fit: cover; }
  /* Blocked image: cover the broken-image icon with the blurred thumbnail, if any */
  .tbd-media img::before { content: ""; position: absolute; inset: -16px;
    background: var(--tbd-ph, none) center / cover no-repeat; filter: blur(8px); }
  .tbd-play { position: absolute; z-index: 2; right: 10px; top: 10px; width: 28px; height: 28px;
    border-radius: 50%; background: rgba(0,0,0,.55); }
  .tbd-play::before { content: ""; position: absolute; left: 11px; top: 8px;
    border-left: 9px solid #fff; border-top: 6px solid transparent; border-bottom: 6px solid transparent; }
  .tbd-head { display: flex; align-items: center; gap: 10px; padding: var(--tbd-pad); }
  .tbd-avatar { flex: none; width: 36px; height: 36px; border-radius: 50%; object-fit: cover;
    background: #d9dbe3; display: grid; place-items: center; font-weight: 700; color: #555; }
  /* Blocked avatar: show the author's initial instead of a broken-image icon */
  img.tbd-avatar { position: relative; }
  img.tbd-avatar::before { content: attr(data-initial); position: absolute; inset: 0; display: grid;
    place-items: center; border-radius: 50%; background: #d9dbe3; }
  .tbd-who { flex: 1; min-width: 0; display: flex; flex-direction: column; text-align: left; }
  .tbd-author { color: var(--tbd-author); font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .tbd-date { font-size: .8em; opacity: .6; }
  .tbd-net { flex: none; width: 24px; height: 24px; border-radius: 50%; display: grid; place-items: center;
    background: #888; color: #fff; font: 700 11px/1 system-ui, sans-serif; }
  .tbd-net::before { content: attr(data-mark); }
  .tbd-net[data-net="instagram"] { background: linear-gradient(45deg, #f9ce34, #ee2a7b 55%, #6228d7); }
  .tbd-net[data-net="facebook"] { background: #1877f2; }
  .tbd-net[data-net="twitter"], .tbd-net[data-net="x"], .tbd-net[data-net="tiktok"] { background: #111; }
  .tbd-net[data-net="youtube"] { background: #ff0000; }
  .tbd-net[data-net="linkedin"] { background: #0a66c2; }
  .tbd-net[data-net="pinterest"] { background: #e60023; }
  .tbd-net[data-net="google"] { background: #4285f4; }
  .tbd-net[data-net="yelp"] { background: #d32323; }
  .tbd-net[data-net="tripadvisor"] { background: #34e0a1; color: #000; }
  .tbd-net[data-net="trustpilot"] { background: #00b67a; }
  .tbd-stars { padding: var(--tbd-pad) var(--tbd-pad) 0; text-align: center; color: #f5b50a; letter-spacing: 2px; }
  .tbd-text { margin: 0; padding: 0 var(--tbd-pad) var(--tbd-pad); line-height: 1.5; }
  .tbd-card > :first-child.tbd-text { padding-top: var(--tbd-pad); }
  .tbd-clamp .tbd-text { display: -webkit-box; -webkit-box-orient: vertical;
    -webkit-line-clamp: var(--tbd-lines); line-clamp: var(--tbd-lines); overflow: hidden; }
  /* Starters only: no posts yet, and the "sample posts" note */
  .tbd-empty, .tbd-note { text-align: center; opacity: .7; margin: 0 0 16px; }

  /* Grid: --tbd-cols columns, 2 on tablet, 1 on phone */
  .tbd-track { display: grid; gap: var(--tbd-gap); align-items: start;
    grid-template-columns: repeat(var(--tbd-cols), minmax(0, 1fr)); }
  @media (max-width: 900px) { .tbd-track { grid-template-columns: repeat(2, minmax(0, 1fr)); } }
  @media (max-width: 560px) { .tbd-track { grid-template-columns: 1fr; } }

  .tbd-card { border-radius: 0; background: none; }
  .tbd-media { aspect-ratio: 1 / 1; }

===== FILE: themes/square-photo.json =====
{
  "name": "Square Photo",
  "slug": "square-photo",
  "type": "social",
  "layout": "grid",
  "parts": [
    "media"
  ],
  "clamp": false,
  "font_url": "https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;600;700&display=swap"
}

===== FILE: themes/story-theme.css =====
/* Story Theme theme - values from themes-lite.json. Change the look here. */
  :root {
    --tbd-bg: #E9EAF0;
    --tbd-surface: #ffffff;
    --tbd-text: #000000;
    --tbd-author: #000000;
    --tbd-font: "Open Sans", system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    --tbd-weight: 400;
    --tbd-size: 14px;
    --tbd-radius: 8px;
    --tbd-img-radius: 0px;
    --tbd-gap: 10px;
    --tbd-pad: 14px;
    --tbd-cols: 3;
    --tbd-align: left;
    --tbd-lines: none;
  }

  * { box-sizing: border-box; }
  body { margin: 0; background: var(--tbd-bg); color: var(--tbd-text);
    font-family: var(--tbd-font); font-weight: var(--tbd-weight); font-size: var(--tbd-size); }
  .tbd-widget { max-width: 1200px; margin: 0 auto; padding: 32px 16px; }
  .tbd-header { margin: 0 0 24px; text-align: center; font-size: 1.9em; font-weight: 700; }
  /* Card: one markup for every theme; the theme block below orders and hides parts */
  .tbd-card { display: flex; flex-direction: column; background: var(--tbd-surface); color: inherit;
    text-decoration: none; border-radius: var(--tbd-radius); overflow: hidden; text-align: var(--tbd-align); }
  /* --tbd-ph: the sample file's tiny base64 thumbnail (preview only; live posts have none) */
  .tbd-media { position: relative; overflow: hidden; border-radius: var(--tbd-img-radius);
    aspect-ratio: var(--tbd-ar, auto);
    background: var(--tbd-ph, none) center / cover no-repeat, linear-gradient(135deg, #e6e8f2, #f3e6ee); }
  /* Fallback tile: chat preview panes block outside images, so show the network name */
  .tbd-media::after { content: attr(data-network); position: absolute; inset: 0; display: grid;
    place-items: center; font-size: .85em; letter-spacing: .06em; text-transform: uppercase; opacity: .45; }
  .tbd-media img, .tbd-media video { position: relative; z-index: 1; display: block; width: 100%; height: 100%; object-fit: cover; }
  /* Blocked image: cover the broken-image icon with the blurred thumbnail, if any */
  .tbd-media img::before { content: ""; position: absolute; inset: -16px;
    background: var(--tbd-ph, none) center / cover no-repeat; filter: blur(8px); }
  .tbd-play { position: absolute; z-index: 2; right: 10px; top: 10px; width: 28px; height: 28px;
    border-radius: 50%; background: rgba(0,0,0,.55); }
  .tbd-play::before { content: ""; position: absolute; left: 11px; top: 8px;
    border-left: 9px solid #fff; border-top: 6px solid transparent; border-bottom: 6px solid transparent; }
  .tbd-head { display: flex; align-items: center; gap: 10px; padding: var(--tbd-pad); }
  .tbd-avatar { flex: none; width: 36px; height: 36px; border-radius: 50%; object-fit: cover;
    background: #d9dbe3; display: grid; place-items: center; font-weight: 700; color: #555; }
  /* Blocked avatar: show the author's initial instead of a broken-image icon */
  img.tbd-avatar { position: relative; }
  img.tbd-avatar::before { content: attr(data-initial); position: absolute; inset: 0; display: grid;
    place-items: center; border-radius: 50%; background: #d9dbe3; }
  .tbd-who { flex: 1; min-width: 0; display: flex; flex-direction: column; text-align: left; }
  .tbd-author { color: var(--tbd-author); font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .tbd-date { font-size: .8em; opacity: .6; }
  .tbd-net { flex: none; width: 24px; height: 24px; border-radius: 50%; display: grid; place-items: center;
    background: #888; color: #fff; font: 700 11px/1 system-ui, sans-serif; }
  .tbd-net::before { content: attr(data-mark); }
  .tbd-net[data-net="instagram"] { background: linear-gradient(45deg, #f9ce34, #ee2a7b 55%, #6228d7); }
  .tbd-net[data-net="facebook"] { background: #1877f2; }
  .tbd-net[data-net="twitter"], .tbd-net[data-net="x"], .tbd-net[data-net="tiktok"] { background: #111; }
  .tbd-net[data-net="youtube"] { background: #ff0000; }
  .tbd-net[data-net="linkedin"] { background: #0a66c2; }
  .tbd-net[data-net="pinterest"] { background: #e60023; }
  .tbd-net[data-net="google"] { background: #4285f4; }
  .tbd-net[data-net="yelp"] { background: #d32323; }
  .tbd-net[data-net="tripadvisor"] { background: #34e0a1; color: #000; }
  .tbd-net[data-net="trustpilot"] { background: #00b67a; }
  .tbd-stars { padding: var(--tbd-pad) var(--tbd-pad) 0; text-align: center; color: #f5b50a; letter-spacing: 2px; }
  .tbd-text { margin: 0; padding: 0 var(--tbd-pad) var(--tbd-pad); line-height: 1.5; }
  .tbd-card > :first-child.tbd-text { padding-top: var(--tbd-pad); }
  .tbd-clamp .tbd-text { display: -webkit-box; -webkit-box-orient: vertical;
    -webkit-line-clamp: var(--tbd-lines); line-clamp: var(--tbd-lines); overflow: hidden; }
  /* Starters only: no posts yet, and the "sample posts" note */
  .tbd-empty, .tbd-note { text-align: center; opacity: .7; margin: 0 0 16px; }

  /* Slider: one row that scrolls sideways; the arrows scroll one view */
  .tbd-slider { position: relative; }
  .tbd-track { display: flex; gap: var(--tbd-gap); overflow-x: auto; scroll-snap-type: x mandatory;
    scrollbar-width: none; padding: 4px 2px 12px; }
  .tbd-track::-webkit-scrollbar { display: none; }
  .tbd-card { flex: 0 0 calc((100% - (var(--tbd-cols) - 1) * var(--tbd-gap)) / var(--tbd-cols));
    scroll-snap-align: start; }
  @media (max-width: 900px) { .tbd-card { flex-basis: calc((100% - var(--tbd-gap)) / 2); } }
  @media (max-width: 560px) { .tbd-card { flex-basis: 85%; } }
  .tbd-arrow { position: absolute; z-index: 4; top: 50%; transform: translateY(-50%); width: 36px; height: 36px;
    border: 0; border-radius: 50%; background: #fff; color: #333; font-size: 20px; cursor: pointer;
    box-shadow: 0 2px 8px rgba(0,0,0,.2); }
  .tbd-arrow--prev { left: -10px; }
  .tbd-arrow--next { right: -10px; }

  .tbd-card { position: relative; background: none; border-radius: 22px; opacity: .45; transform: scale(.92);
    transition: opacity .2s, transform .2s; }
  /* The middle story is in focus; the others fade until hovered */
  .tbd-card:nth-child(2), .tbd-card:hover, .tbd-card:focus-visible { opacity: 1; transform: none; }
  .tbd-media { aspect-ratio: 9 / 15; border-radius: 22px; }
  .tbd-head { position: absolute; z-index: 2; left: 0; right: 0; bottom: 0; flex-direction: column; gap: 4px;
    padding-bottom: 18px; color: #fff; background: linear-gradient(transparent, rgba(0,0,0,.65)); border-radius: 0 0 22px 22px; }
  .tbd-who { text-align: center; align-items: center; }
  .tbd-author { color: #fff; }
  .tbd-avatar { width: 48px; height: 48px; border: 3px solid #fff; }

===== FILE: themes/story-theme.json =====
{
  "name": "Story Theme",
  "slug": "story-theme",
  "type": "social",
  "layout": "slider",
  "parts": [
    "media",
    "head"
  ],
  "clamp": false,
  "font_url": "https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;600;700&display=swap"
}

===== FILE: themes/vivid.css =====
/* Vivid theme - values from themes-lite.json. Change the look here. */
  :root {
    --tbd-bg: #ffffff;
    --tbd-surface: #fafafa;
    --tbd-text: #1f1b1b;
    --tbd-author: #1f1b1b;
    --tbd-font: system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    --tbd-weight: 400;
    --tbd-size: 14px;
    --tbd-radius: 3px;
    --tbd-img-radius: 0px;
    --tbd-gap: 10px;
    --tbd-pad: 14px;
    --tbd-cols: 4;
    --tbd-align: left;
    --tbd-lines: 4;
  }

  * { box-sizing: border-box; }
  body { margin: 0; background: var(--tbd-bg); color: var(--tbd-text);
    font-family: var(--tbd-font); font-weight: var(--tbd-weight); font-size: var(--tbd-size); }
  .tbd-widget { max-width: 1200px; margin: 0 auto; padding: 32px 16px; }
  .tbd-header { margin: 0 0 24px; text-align: center; font-size: 1.9em; font-weight: 700; }
  /* Card: one markup for every theme; the theme block below orders and hides parts */
  .tbd-card { display: flex; flex-direction: column; background: var(--tbd-surface); color: inherit;
    text-decoration: none; border-radius: var(--tbd-radius); overflow: hidden; text-align: var(--tbd-align); }
  /* --tbd-ph: the sample file's tiny base64 thumbnail (preview only; live posts have none) */
  .tbd-media { position: relative; overflow: hidden; border-radius: var(--tbd-img-radius);
    aspect-ratio: var(--tbd-ar, auto);
    background: var(--tbd-ph, none) center / cover no-repeat, linear-gradient(135deg, #e6e8f2, #f3e6ee); }
  /* Fallback tile: chat preview panes block outside images, so show the network name */
  .tbd-media::after { content: attr(data-network); position: absolute; inset: 0; display: grid;
    place-items: center; font-size: .85em; letter-spacing: .06em; text-transform: uppercase; opacity: .45; }
  .tbd-media img, .tbd-media video { position: relative; z-index: 1; display: block; width: 100%; height: 100%; object-fit: cover; }
  /* Blocked image: cover the broken-image icon with the blurred thumbnail, if any */
  .tbd-media img::before { content: ""; position: absolute; inset: -16px;
    background: var(--tbd-ph, none) center / cover no-repeat; filter: blur(8px); }
  .tbd-play { position: absolute; z-index: 2; right: 10px; top: 10px; width: 28px; height: 28px;
    border-radius: 50%; background: rgba(0,0,0,.55); }
  .tbd-play::before { content: ""; position: absolute; left: 11px; top: 8px;
    border-left: 9px solid #fff; border-top: 6px solid transparent; border-bottom: 6px solid transparent; }
  .tbd-head { display: flex; align-items: center; gap: 10px; padding: var(--tbd-pad); }
  .tbd-avatar { flex: none; width: 36px; height: 36px; border-radius: 50%; object-fit: cover;
    background: #d9dbe3; display: grid; place-items: center; font-weight: 700; color: #555; }
  /* Blocked avatar: show the author's initial instead of a broken-image icon */
  img.tbd-avatar { position: relative; }
  img.tbd-avatar::before { content: attr(data-initial); position: absolute; inset: 0; display: grid;
    place-items: center; border-radius: 50%; background: #d9dbe3; }
  .tbd-who { flex: 1; min-width: 0; display: flex; flex-direction: column; text-align: left; }
  .tbd-author { color: var(--tbd-author); font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .tbd-date { font-size: .8em; opacity: .6; }
  .tbd-net { flex: none; width: 24px; height: 24px; border-radius: 50%; display: grid; place-items: center;
    background: #888; color: #fff; font: 700 11px/1 system-ui, sans-serif; }
  .tbd-net::before { content: attr(data-mark); }
  .tbd-net[data-net="instagram"] { background: linear-gradient(45deg, #f9ce34, #ee2a7b 55%, #6228d7); }
  .tbd-net[data-net="facebook"] { background: #1877f2; }
  .tbd-net[data-net="twitter"], .tbd-net[data-net="x"], .tbd-net[data-net="tiktok"] { background: #111; }
  .tbd-net[data-net="youtube"] { background: #ff0000; }
  .tbd-net[data-net="linkedin"] { background: #0a66c2; }
  .tbd-net[data-net="pinterest"] { background: #e60023; }
  .tbd-net[data-net="google"] { background: #4285f4; }
  .tbd-net[data-net="yelp"] { background: #d32323; }
  .tbd-net[data-net="tripadvisor"] { background: #34e0a1; color: #000; }
  .tbd-net[data-net="trustpilot"] { background: #00b67a; }
  .tbd-stars { padding: var(--tbd-pad) var(--tbd-pad) 0; text-align: center; color: #f5b50a; letter-spacing: 2px; }
  .tbd-text { margin: 0; padding: 0 var(--tbd-pad) var(--tbd-pad); line-height: 1.5; }
  .tbd-card > :first-child.tbd-text { padding-top: var(--tbd-pad); }
  .tbd-clamp .tbd-text { display: -webkit-box; -webkit-box-orient: vertical;
    -webkit-line-clamp: var(--tbd-lines); line-clamp: var(--tbd-lines); overflow: hidden; }
  /* Starters only: no posts yet, and the "sample posts" note */
  .tbd-empty, .tbd-note { text-align: center; opacity: .7; margin: 0 0 16px; }

  /* Masonry: CSS columns, each card keeps its own height */
  .tbd-track { column-count: var(--tbd-cols); column-gap: var(--tbd-gap); }
  .tbd-card { break-inside: avoid; margin-bottom: var(--tbd-gap); }
  @media (max-width: 900px) { .tbd-track { column-count: 2; } }
  @media (max-width: 560px) { .tbd-track { column-count: 1; } }

  /* Each card gets one of six soft gradients; text-only cards are fully tinted */
  .tbd-card { background: var(--tbd-tint); }
  .tbd-card:nth-child(6n + 1) { --tbd-tint: linear-gradient(135deg, #fff1a8, #ffd98a); }
  .tbd-card:nth-child(6n + 2) { --tbd-tint: linear-gradient(135deg, #b8f0c8, #8fdcc0); }
  .tbd-card:nth-child(6n + 3) { --tbd-tint: linear-gradient(135deg, #ffd0b8, #f2a98f); }
  .tbd-card:nth-child(6n + 4) { --tbd-tint: linear-gradient(135deg, #a9c8ff, #5f8ef2); }
  .tbd-card:nth-child(6n + 5) { --tbd-tint: linear-gradient(135deg, #c3eaff, #9fd6f7); }
  .tbd-card:nth-child(6n + 6) { --tbd-tint: linear-gradient(135deg, #ffb3c7, #e8708f); }
  .tbd-media { aspect-ratio: 1 / 1; }

===== FILE: themes/vivid.json =====
{
  "name": "Vivid",
  "slug": "vivid",
  "type": "social",
  "layout": "masonry",
  "parts": [
    "media",
    "head",
    "text"
  ],
  "clamp": true,
  "font_url": ""
}

===== FILE: themes/widget-theme.css =====
/* Widget Theme theme - values from themes-lite.json. Change the look here. */
  :root {
    --tbd-bg: #E9EAF0;
    --tbd-surface: #ffffff;
    --tbd-text: #2B2B2B;
    --tbd-author: #2B2B2B;
    --tbd-font: "Inter", system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    --tbd-weight: 400;
    --tbd-size: 14px;
    --tbd-radius: 3px;
    --tbd-img-radius: 0px;
    --tbd-gap: 10px;
    --tbd-pad: 12px;
    --tbd-cols: 1;
    --tbd-align: center;
    --tbd-lines: 5;
  }

  * { box-sizing: border-box; }
  body { margin: 0; background: var(--tbd-bg); color: var(--tbd-text);
    font-family: var(--tbd-font); font-weight: var(--tbd-weight); font-size: var(--tbd-size); }
  .tbd-widget { max-width: 1200px; margin: 0 auto; padding: 32px 16px; }
  .tbd-header { margin: 0 0 24px; text-align: center; font-size: 1.9em; font-weight: 700; }
  /* Card: one markup for every theme; the theme block below orders and hides parts */
  .tbd-card { display: flex; flex-direction: column; background: var(--tbd-surface); color: inherit;
    text-decoration: none; border-radius: var(--tbd-radius); overflow: hidden; text-align: var(--tbd-align); }
  /* --tbd-ph: the sample file's tiny base64 thumbnail (preview only; live posts have none) */
  .tbd-media { position: relative; overflow: hidden; border-radius: var(--tbd-img-radius);
    aspect-ratio: var(--tbd-ar, auto);
    background: var(--tbd-ph, none) center / cover no-repeat, linear-gradient(135deg, #e6e8f2, #f3e6ee); }
  /* Fallback tile: chat preview panes block outside images, so show the network name */
  .tbd-media::after { content: attr(data-network); position: absolute; inset: 0; display: grid;
    place-items: center; font-size: .85em; letter-spacing: .06em; text-transform: uppercase; opacity: .45; }
  .tbd-media img, .tbd-media video { position: relative; z-index: 1; display: block; width: 100%; height: 100%; object-fit: cover; }
  /* Blocked image: cover the broken-image icon with the blurred thumbnail, if any */
  .tbd-media img::before { content: ""; position: absolute; inset: -16px;
    background: var(--tbd-ph, none) center / cover no-repeat; filter: blur(8px); }
  .tbd-play { position: absolute; z-index: 2; right: 10px; top: 10px; width: 28px; height: 28px;
    border-radius: 50%; background: rgba(0,0,0,.55); }
  .tbd-play::before { content: ""; position: absolute; left: 11px; top: 8px;
    border-left: 9px solid #fff; border-top: 6px solid transparent; border-bottom: 6px solid transparent; }
  .tbd-head { display: flex; align-items: center; gap: 10px; padding: var(--tbd-pad); }
  .tbd-avatar { flex: none; width: 36px; height: 36px; border-radius: 50%; object-fit: cover;
    background: #d9dbe3; display: grid; place-items: center; font-weight: 700; color: #555; }
  /* Blocked avatar: show the author's initial instead of a broken-image icon */
  img.tbd-avatar { position: relative; }
  img.tbd-avatar::before { content: attr(data-initial); position: absolute; inset: 0; display: grid;
    place-items: center; border-radius: 50%; background: #d9dbe3; }
  .tbd-who { flex: 1; min-width: 0; display: flex; flex-direction: column; text-align: left; }
  .tbd-author { color: var(--tbd-author); font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .tbd-date { font-size: .8em; opacity: .6; }
  .tbd-net { flex: none; width: 24px; height: 24px; border-radius: 50%; display: grid; place-items: center;
    background: #888; color: #fff; font: 700 11px/1 system-ui, sans-serif; }
  .tbd-net::before { content: attr(data-mark); }
  .tbd-net[data-net="instagram"] { background: linear-gradient(45deg, #f9ce34, #ee2a7b 55%, #6228d7); }
  .tbd-net[data-net="facebook"] { background: #1877f2; }
  .tbd-net[data-net="twitter"], .tbd-net[data-net="x"], .tbd-net[data-net="tiktok"] { background: #111; }
  .tbd-net[data-net="youtube"] { background: #ff0000; }
  .tbd-net[data-net="linkedin"] { background: #0a66c2; }
  .tbd-net[data-net="pinterest"] { background: #e60023; }
  .tbd-net[data-net="google"] { background: #4285f4; }
  .tbd-net[data-net="yelp"] { background: #d32323; }
  .tbd-net[data-net="tripadvisor"] { background: #34e0a1; color: #000; }
  .tbd-net[data-net="trustpilot"] { background: #00b67a; }
  .tbd-stars { padding: var(--tbd-pad) var(--tbd-pad) 0; text-align: center; color: #f5b50a; letter-spacing: 2px; }
  .tbd-text { margin: 0; padding: 0 var(--tbd-pad) var(--tbd-pad); line-height: 1.5; }
  .tbd-card > :first-child.tbd-text { padding-top: var(--tbd-pad); }
  .tbd-clamp .tbd-text { display: -webkit-box; -webkit-box-orient: vertical;
    -webkit-line-clamp: var(--tbd-lines); line-clamp: var(--tbd-lines); overflow: hidden; }
  /* Starters only: no posts yet, and the "sample posts" note */
  .tbd-empty, .tbd-note { text-align: center; opacity: .7; margin: 0 0 16px; }

  /* List: one column */
  .tbd-track { display: grid; gap: var(--tbd-gap); max-width: 640px; margin: 0 auto; }

  .tbd-track { gap: 32px; }
  .tbd-card { text-align: center; }
  .tbd-text { padding: var(--tbd-pad) 24px 24px; }

===== FILE: themes/widget-theme.json =====
{
  "name": "Widget Theme",
  "slug": "widget-theme",
  "type": "social",
  "layout": "list",
  "parts": [
    "head",
    "media",
    "text"
  ],
  "clamp": true,
  "font_url": "https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap"
}

===== FILE: samples/reviews.json =====
[
  {
    "id": "post_9101",
    "object": "post",
    "wall_id": "wall_123",
    "feed_id": "feed_20",
    "album_id": null,
    "network": {
      "id": "network_10",
      "slug": "google",
      "name": "Google"
    },
    "media_type": "text",
    "pinned": true,
    "active": true,
    "created_at": "2026-09-20T09:02:00.000Z",
    "created_timestamp": 1789894920,
    "modified_at": null,
    "modified_timestamp": null,
    "content": {
      "title": null,
      "text": "Fast shipping, and the quality is even better than I expected. Will definitely order again."
    },
    "media": [],
    "author": {
      "name": "Sara Kim",
      "handle": null,
      "avatar_url": "https://images.unsplash.com/photo-1534528741775-53994a69daeb?w=150&h=150&fit=crop&crop=faces&auto=format&q=80",
      "profile_url": null,
      "external_id": "g_301944"
    },
    "source": {
      "external_post_id": "ChIJ9101sample",
      "permalink": "https://g.page/r/sample9101/review"
    },
    "counts": {
      "likes": 4,
      "comments": 0
    },
    "sentiment": "positive",
    "rating": 5,
    "products": null
  },
  {
    "id": "post_9102",
    "object": "post",
    "wall_id": "wall_123",
    "feed_id": "feed_21",
    "album_id": null,
    "network": {
      "id": "network_11",
      "slug": "yelp",
      "name": "Yelp"
    },
    "media_type": "text",
    "pinned": false,
    "active": true,
    "created_at": "2026-09-18T15:40:00.000Z",
    "created_timestamp": 1789746000,
    "modified_at": null,
    "modified_timestamp": null,
    "content": {
      "title": null,
      "text": "Good experience overall. Staff was friendly and the place was clean. Only reason it's not 5 stars is the wait time."
    },
    "media": [],
    "author": {
      "name": "Marcus T.",
      "handle": null,
      "avatar_url": "https://images.unsplash.com/photo-1506794778202-cad84cf45f1d?w=150&h=150&fit=crop&crop=faces&auto=format&q=80",
      "profile_url": "https://yelp.com/user/marcust",
      "external_id": "yelp_18823"
    },
    "source": {
      "external_post_id": "9102sample",
      "permalink": "https://yelp.com/biz/sample-store#9102sample"
    },
    "counts": {
      "likes": 2,
      "comments": 1
    },
    "sentiment": "positive",
    "rating": 4,
    "products": null
  },
  {
    "id": "post_9103",
    "object": "post",
    "wall_id": "wall_123",
    "feed_id": "feed_22",
    "album_id": null,
    "network": {
      "id": "network_12",
      "slug": "tripadvisor",
      "name": "Tripadvisor"
    },
    "media_type": "image",
    "pinned": false,
    "active": true,
    "created_at": "2026-09-16T12:10:00.000Z",
    "created_timestamp": 1789560600,
    "modified_at": null,
    "modified_timestamp": null,
    "content": {
      "title": "Exceeded expectations",
      "text": "Everything from check-in to check-out was smooth. Adding a couple of photos from our stay."
    },
    "media": [
      {
        "type": "image",
        "url": "https://images.unsplash.com/photo-1566073771259-6a8506099945?w=1024&h=768&fit=crop&auto=format&q=80",
        "cdn_url": "https://images.unsplash.com/photo-1566073771259-6a8506099945?w=1024&h=768&fit=crop&auto=format&q=80",
        "width": 1024,
        "height": 768
      }
    ],
    "author": {
      "name": "The Alvarez Family",
      "handle": null,
      "avatar_url": "https://images.unsplash.com/photo-1529156069898-49953e39b3ac?w=150&h=150&fit=crop&crop=faces&auto=format&q=80",
      "profile_url": null,
      "external_id": "ta_50221"
    },
    "source": {
      "external_post_id": "9103sample",
      "permalink": "https://tripadvisor.com/ShowUserReviews-9103sample"
    },
    "counts": {
      "likes": 9,
      "comments": 0
    },
    "sentiment": "positive",
    "rating": 5,
    "products": null
  },
  {
    "id": "post_9104",
    "object": "post",
    "wall_id": "wall_123",
    "feed_id": "feed_23",
    "album_id": null,
    "network": {
      "id": "network_1",
      "slug": "facebook",
      "name": "Facebook"
    },
    "media_type": "text",
    "pinned": false,
    "active": true,
    "created_at": "2026-09-14T18:25:00.000Z",
    "created_timestamp": 1789410300,
    "modified_at": null,
    "modified_timestamp": null,
    "content": {
      "title": null,
      "text": "It's decent for the price, but delivery took longer than the estimate I was given."
    },
    "media": [],
    "author": {
      "name": "Neha Joshi",
      "handle": null,
      "avatar_url": "https://images.unsplash.com/photo-1580489944761-15a19d654956?w=150&h=150&fit=crop&crop=faces&auto=format&q=80",
      "profile_url": "https://facebook.com/neha.joshi.77",
      "external_id": "fb_66102"
    },
    "source": {
      "external_post_id": "9104sample",
      "permalink": "https://facebook.com/ourbrandpage/reviews/9104sample"
    },
    "counts": {
      "likes": 0,
      "comments": 0
    },
    "sentiment": "neutral",
    "rating": 3,
    "products": null
  },
  {
    "id": "post_9105",
    "object": "post",
    "wall_id": "wall_123",
    "feed_id": "feed_20",
    "album_id": null,
    "network": {
      "id": "network_10",
      "slug": "google",
      "name": "Google"
    },
    "media_type": "text",
    "pinned": false,
    "active": true,
    "created_at": "2026-09-12T07:55:00.000Z",
    "created_timestamp": 1789199700,
    "modified_at": null,
    "modified_timestamp": null,
    "content": {
      "title": null,
      "text": "Best customer service I've dealt with all year. They went out of their way to fix a sizing issue."
    },
    "media": [],
    "author": {
      "name": "Tom Becker",
      "handle": null,
      "avatar_url": "https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=150&h=150&fit=crop&crop=faces&auto=format&q=80",
      "profile_url": null,
      "external_id": "g_44190"
    },
    "source": {
      "external_post_id": "ChIJ9105sample",
      "permalink": "https://g.page/r/sample9105/review"
    },
    "counts": {
      "likes": 6,
      "comments": 0
    },
    "sentiment": "positive",
    "rating": 5,
    "products": null
  },
  {
    "id": "post_9106",
    "object": "post",
    "wall_id": "wall_123",
    "feed_id": "feed_24",
    "album_id": null,
    "network": {
      "id": "network_13",
      "slug": "trustpilot",
      "name": "Trustpilot"
    },
    "media_type": "text",
    "pinned": false,
    "active": true,
    "created_at": "2026-09-09T20:30:00.000Z",
    "created_timestamp": 1788985800,
    "modified_at": null,
    "modified_timestamp": null,
    "content": {
      "title": "Not what I expected",
      "text": "Package arrived damaged and support took three days to respond. Hoping this was a one-off."
    },
    "media": [],
    "author": {
      "name": "L. Petrova",
      "handle": null,
      "avatar_url": "https://images.unsplash.com/photo-1438761681033-6461ffad8d80?w=150&h=150&fit=crop&crop=faces&auto=format&q=80",
      "profile_url": "https://trustpilot.com/users/petrova",
      "external_id": "tp_30187"
    },
    "source": {
      "external_post_id": "9106sample",
      "permalink": "https://trustpilot.com/reviews/9106sample"
    },
    "counts": {
      "likes": 1,
      "comments": 1
    },
    "sentiment": "negative",
    "rating": 2,
    "products": null
  }
]

===== FILE: samples/social.json =====
[
  {
    "id": "post_9001",
    "object": "post",
    "wall_id": "wall_123",
    "feed_id": "feed_10",
    "album_id": null,
    "network": {
      "id": "network_2",
      "slug": "instagram",
      "name": "Instagram"
    },
    "media_type": "image",
    "pinned": true,
    "active": true,
    "created_at": "2026-09-20T14:32:10.000Z",
    "created_timestamp": 1789914730,
    "modified_at": null,
    "modified_timestamp": null,
    "content": {
      "title": null,
      "text": "Golden hour at the flagship store — thank you for stopping by! 🌞"
    },
    "media": [
      {
        "type": "image",
        "url": "https://cloud.tagbox.com/media/141712/2231424/561924/17888143008614381/810182212_18113135807127332_4549521118308771593_n.jpg",
        "cdn_url": "https://cloud.tagbox.com/media/141712/2231424/561924/17888143008614381/810182212_18113135807127332_4549521118308771593_n.jpg",
        "width": 1080,
        "height": 1080
      }
    ],
    "author": {
      "name": "Maya Fernandes",
      "handle": "maya.fernandes",
      "avatar_url": "https://images.unsplash.com/photo-1494790108377-be9c29b29330?w=150&h=150&fit=crop&crop=faces&auto=format&q=80",
      "profile_url": "https://instagram.com/maya.fernandes",
      "external_id": "ig_10029"
    },
    "source": {
      "external_post_id": "17895123456",
      "permalink": "https://instagram.com/p/Cx9001Sample"
    },
    "counts": {
      "likes": 342,
      "comments": 18
    },
    "sentiment": "positive",
    "rating": null,
    "products": null
  },
  {
    "id": "post_9002",
    "object": "post",
    "wall_id": "wall_123",
    "feed_id": "feed_11",
    "album_id": null,
    "network": {
      "id": "network_5",
      "slug": "tiktok",
      "name": "TikTok"
    },
    "media_type": "video",
    "pinned": false,
    "active": true,
    "created_at": "2026-09-19T08:15:44.000Z",
    "created_timestamp": 1789805744,
    "modified_at": null,
    "modified_timestamp": null,
    "content": {
      "title": null,
      "text": "Caught the restock drop before it sold out 🛒 third order this year and it never disappoints"
    },
    "media": [
      {
        "type": "video",
        "url": "https://cloud.tagbox.com/media/141712/2231424/561924/18103532516193855/playlist.m3u8",
        "cdn_url": "https://cloud.tagbox.com/media/141712/2231424/561924/18103532516193855/playlist.m3u8",
        "width": 720,
        "height": 720
      },
      {
        "type": "image",
        "url": "https://cloud.tagbox.com/media/141712/2231424/561924/18103532516193855/thumb_v7i8h3mu.jpg",
        "cdn_url": "https://cloud.tagbox.com/media/141712/2231424/561924/18103532516193855/thumb_v7i8h3mu.jpg",
        "width": 720,
        "height": 720
      }
    ],
    "author": {
      "name": "Devon Cole",
      "handle": "@devoncole",
      "avatar_url": "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=150&h=150&fit=crop&crop=faces&auto=format&q=80",
      "profile_url": "https://tiktok.com/@devoncole",
      "external_id": "tt_88213"
    },
    "source": {
      "external_post_id": "7301123456",
      "permalink": "https://tiktok.com/@devoncole/video/9002sample"
    },
    "counts": {
      "likes": 1204,
      "comments": 76
    },
    "sentiment": "positive",
    "rating": null,
    "products": null
  },
  {
    "id": "post_9003",
    "object": "post",
    "wall_id": "wall_123",
    "feed_id": "feed_12",
    "album_id": null,
    "network": {
      "id": "network_1",
      "slug": "facebook",
      "name": "Facebook"
    },
    "media_type": "image",
    "pinned": false,
    "active": true,
    "created_at": "2026-09-18T19:05:02.000Z",
    "created_timestamp": 1789758302,
    "modified_at": null,
    "modified_timestamp": null,
    "content": {
      "title": null,
      "text": "Had such a great time at the pop-up this weekend. See you all next month!"
    },
    "media": [
      {
        "type": "image",
        "url": "https://cloud.tagbox.com/media/141712/2231424/561924/18088373651668689/801181501_18112526939127332_1972507973509491791_n.jpg",
        "cdn_url": "https://cloud.tagbox.com/media/141712/2231424/561924/18088373651668689/801181501_18112526939127332_1972507973509491791_n.jpg",
        "width": 594,
        "height": 594
      }
    ],
    "author": {
      "name": "Priya Nair",
      "handle": null,
      "avatar_url": "https://images.unsplash.com/photo-1488426862026-3ee34a7d66df?w=150&h=150&fit=crop&crop=faces&auto=format&q=80",
      "profile_url": "https://facebook.com/priya.nair.98",
      "external_id": "fb_55210"
    },
    "source": {
      "external_post_id": "1029384756",
      "permalink": "https://facebook.com/priya.nair.98/posts/9003sample"
    },
    "counts": {
      "likes": 89,
      "comments": 6
    },
    "sentiment": "positive",
    "rating": null,
    "products": null
  },
  {
    "id": "post_9004",
    "object": "post",
    "wall_id": "wall_123",
    "feed_id": "feed_13",
    "album_id": null,
    "network": {
      "id": "network_4",
      "slug": "twitter",
      "name": "X (Twitter)"
    },
    "media_type": "text",
    "pinned": false,
    "active": true,
    "created_at": "2026-09-17T11:48:27.000Z",
    "created_timestamp": 1789645707,
    "modified_at": null,
    "modified_timestamp": null,
    "content": {
      "title": null,
      "text": "Customer support just resolved my issue in under 10 minutes. That's how it's done."
    },
    "media": [],
    "author": {
      "name": "Alex Whitmore",
      "handle": "@alexw",
      "avatar_url": "https://images.unsplash.com/photo-1500648767791-00dcc994a43e?w=150&h=150&fit=crop&crop=faces&auto=format&q=80",
      "profile_url": "https://x.com/alexw",
      "external_id": "tw_44102"
    },
    "source": {
      "external_post_id": "1789645707000001",
      "permalink": "https://x.com/alexw/status/9004sample"
    },
    "counts": {
      "likes": 54,
      "comments": 3
    },
    "sentiment": "positive",
    "rating": null,
    "products": null
  },
  {
    "id": "post_9005",
    "object": "post",
    "wall_id": "wall_123",
    "feed_id": "feed_14",
    "album_id": null,
    "network": {
      "id": "network_6",
      "slug": "youtube",
      "name": "YouTube"
    },
    "media_type": "video",
    "pinned": false,
    "active": true,
    "created_at": "2026-09-16T21:12:56.000Z",
    "created_timestamp": 1789593176,
    "modified_at": null,
    "modified_timestamp": null,
    "content": {
      "title": "My honest first impressions",
      "text": "Full walkthrough of everything in the box, plus my honest first impressions after a week of daily use."
    },
    "media": [
      {
        "type": "video",
        "url": "https://cloud.tagbox.com/media/141712/2231424/561924/17984344209062084/playlist.m3u8",
        "cdn_url": "https://cloud.tagbox.com/media/141712/2231424/561924/17984344209062084/playlist.m3u8",
        "width": 720,
        "height": 720
      },
      {
        "type": "image",
        "url": "https://cloud.tagbox.com/media/141712/2231424/561924/17984344209062084/thumb_i389dkb4.jpg",
        "cdn_url": "https://cloud.tagbox.com/media/141712/2231424/561924/17984344209062084/thumb_i389dkb4.jpg",
        "width": 720,
        "height": 720
      }
    ],
    "author": {
      "name": "The Weekend Reviewer",
      "handle": "@weekendreviewer",
      "avatar_url": "https://images.unsplash.com/photo-1547425260-76bcadfb4f2c?w=150&h=150&fit=crop&crop=faces&auto=format&q=80",
      "profile_url": "https://youtube.com/@weekendreviewer",
      "external_id": "yt_20391"
    },
    "source": {
      "external_post_id": "yt_9005sample",
      "permalink": "https://youtube.com/watch?v=9005sample"
    },
    "counts": {
      "likes": 610,
      "comments": 42
    },
    "sentiment": "positive",
    "rating": null,
    "products": null
  },
  {
    "id": "post_9006",
    "object": "post",
    "wall_id": "wall_123",
    "feed_id": "feed_15",
    "album_id": null,
    "network": {
      "id": "network_7",
      "slug": "linkedin",
      "name": "LinkedIn"
    },
    "media_type": "image",
    "pinned": false,
    "active": true,
    "created_at": "2026-09-15T09:40:33.000Z",
    "created_timestamp": 1789465233,
    "modified_at": null,
    "modified_timestamp": null,
    "content": {
      "title": null,
      "text": "Proud to share that our team shipped this integration a full sprint ahead of schedule."
    },
    "media": [
      {
        "type": "image",
        "url": "https://cloud.tagbox.com/media/141712/2231424/561924/17901115977202806/616461587_18087833159127332_2981774030571158343_n.jpg",
        "cdn_url": "https://cloud.tagbox.com/media/141712/2231424/561924/17901115977202806/616461587_18087833159127332_2981774030571158343_n.jpg",
        "width": 1440,
        "height": 1440
      }
    ],
    "author": {
      "name": "Rahul Menon",
      "handle": null,
      "avatar_url": "https://images.unsplash.com/photo-1531427186611-ecfd6d936c79?w=150&h=150&fit=crop&crop=faces&auto=format&q=80",
      "profile_url": "https://linkedin.com/in/rahulmenon",
      "external_id": "li_77341"
    },
    "source": {
      "external_post_id": "urn:li:activity:9006sample",
      "permalink": "https://linkedin.com/feed/update/urn:li:activity:9006sample"
    },
    "counts": {
      "likes": 128,
      "comments": 11
    },
    "sentiment": null,
    "rating": null,
    "products": null
  }
]
